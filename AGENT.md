# AGENT.md — RetroAI-Scaler 实现说明

给后续开发者/AI 代理看的技术文档。**README 讲功能，这里讲为什么这么实现**，尤其是那些"看起来可以更简单，但实际会炸"的地方。

目标平台：Android 11+ 掌机（Helio G90T 起步）。实测机型两台：AYANEO Air Mini（mt6785 / Helio G95 / Android 11 / 1280×960 / 60Hz）与 AYANEO Pocket FIT Elite（SM8750P / Adreno 840 / Android 16 / 1080×1920 竖向面板 / 144Hz）。

**跨机型的差异集中在第 10 节**，动线程、EGL、取景窗探测或覆盖层窗口之前先读那一节——那里的每一条都是"在一台机器上一直是对的、换一台就炸"的假设。

## 当前状态（2026-08-19）

功能主体完成并在真机上验证过。master 已同步，CI 绿（`.github/workflows/build.yml`，推送即出 debug APK）。

- **放大引擎**：像素边缘重建 / HD-2D 光影 / GPU 锐化 / ESPCN Fast·HQ·Ultra，倍率 1·2·3·4·6
- **HD-2D**（第 13 节）：深度打光 + 移轴景深 + 泛光，合成为一个引擎预设，无参数暴露。**移动时的闪烁已解决（13.11），根因是平移不变性缺失而非时域噪声——13.3③ 那条"没有免费午餐"的结论已作废。**
- **模型仓库**已公开：[RetroAI-Model](https://github.com/sankerXD/RetroAI-Model)，训练脚本、语料处理、导出器都在那边

未做：**第 9 节的 release 签名和更多遮罩类型**。

---

## 1. 架构的硬约束：取景区和输出区不能重叠

`MediaProjection` 镜像的是**整块屏幕，包含本应用自己画的覆盖层**。由此推出：

- 输出盖在取景区上 → 下一帧捕获到自己的输出 → 反馈自激，几帧内画面糊掉；
- 给覆盖层加 `FLAG_SECURE` 想让录屏跳过它 → **SurfaceFlinger 会把该图层在镜像里替换成不透明黑块**（不是跳过）。全屏覆盖层 = 整帧纯黑 = 整机黑屏，且覆盖层置于所有应用之上，用户无法自救。
- 安卓 14 的单应用捕获（`MediaProjectionConfig.createConfigForUserChoice()`）能绕开，但**那是 API 34**，安卓 12/13 都没有。

所以：**RetroArch 缩到屏幕一角以原生分辨率绘制（取景窗），增强画面放在剩余空白区**。着色器对取景窗区域 `discard`，输出区外一律 `discard`。

安全底线（任何改动都不能破坏）：

1. 覆盖层**永不** `FLAG_SECURE`，**永不** `setZOrderOnTop(true)`（后者会盖住悬浮球和菜单，用户无路可退）；
2. 任何异常状态（几何未配置、着色器编译失败、无帧、退出中）一律退化为**全透明**，绝不是黑屏；
3. 通知栏常驻【停止】；崩溃恢复命令 `adb shell am force-stop com.retroai.scaler`，或重启设备（服务不自启）。

### 只在目标应用前台时渲染

覆盖层是全屏 `TYPE_APPLICATION_OVERLAY`，不加限制会盖住桌面和一切应用（触摸能穿透，但什么都看不见，表现为"切不回去"）。

`ForegroundAppMonitor` 用 `UsageStatsManager` 每 400ms 轮询前台应用（需「使用情况访问」特殊权限）。目标应用离开 → 擦成全透明 + `virtualDisplay.setSurface(null)` 挂起镜像。

> **踩坑**：挂起**必须**用 `setSurface(null)`，不能 release VirtualDisplay 再重建——重建出来的 VirtualDisplay 不会再往已有的 ImageReader surface 投帧，一帧收不到，看门狗会判定管线已死并关服务。

---

## 2. RetroArch 配置：两条互斥的代码路径

写配置前必读，否则会像最初那样把画面搞消失。

**主配置 `retroarch.cfg` 在 `Android/data/<pkg>/files/` 下，安卓 11 起任何第三方应用都无法访问**，MANAGE_EXTERNAL_STORAGE 也不覆盖。可写的只有覆盖配置目录 `rgui_config_directory`（默认 `/storage/emulated/0/RetroArch/config`），配合 `auto_overrides_enable=true` 生效。

RA 的覆盖优先级是 **核心 → 内容目录 → 单游戏**，所以要写核心目录下**每一个 .cfg**，只改核心那个会被后面的盖回去。

### `video_scale_integer` 决定另外四个键的含义

| `video_scale_integer` | 行为 |
|---|---|
| `"true"` | `custom_viewport_width/height` 只当**包围盒**，算出能塞进去的最大整数倍，位置由 **`video_viewport_bias_x/y`**（归一化 0~1）决定。**`custom_viewport_x/y` 完全不被读取。** |
| `"false"` | 直接用 `custom_viewport_x/y/width/height` 作原始矩形，坐标在**视频驱动的坐标系**里——GL 是左下原点，Vulkan 不同。 |

**实测教训**：按 GL 约定算 `custom_viewport_x/y` 喂给 Vulkan 驱动（本机 `video_driver=vulkan`），结果整屏纯黑（像素级验证除 HUD 外全是 0）。因此本项目**只写 bias，绝不写 x/y**，并保持整数缩放开启——顺带保证像素级 1:1 采样，这是超分最好的输入。

实际写入的键（`RetroArchConfigManager`）：

```
aspect_ratio_index    = "23"     # 自定义比例，唯一认视口尺寸的模式
video_scale_integer   = "true"
custom_viewport_width/height     # 原生分辨率 × sourceScale
video_viewport_bias_x/y          # 角落位置，无坐标原点歧义
input_overlay_enable  = "false"  # 整合包机型边框是全屏 input overlay，会被采进取景窗
video_shader_enable   = "false"  # RA 着色器在我们之前跑，等于喂给网络一张已滤过的图
```

> 关掉着色器**已验证安全**（单变量测试：只加这一行，画面正常，仅 LCD 网格效果消失）。黑屏的元凶是 `video_scale_integer=false` + `custom_viewport_x/y` 那一组。

### 位置靠测，不靠算

写配置只控制**尺寸和大致角落**；实际采样位置由 `nativeRequestSourceDetection()` 从捕获帧里**量出来**：

1. 先把自己的覆盖层、悬浮球、贴边条、菜单全部撤掉，空 4 帧（否则会拍到自己）；
2. 整屏缩到 320×240 回读，阈值化后做 8 连通域标记；
3. 候选先过**长宽比闸门**（与原生比例差 >8% 淘汰），再匹配原生分辨率的**某个整数倍**（1~8x 全试，偏差 >35% 淘汰），剩下取面积最大者；
4. 结果**吸附到 `native × k`**，只保留实测中心作为位置。

> 第 3、4 步的细节和它们各自防的是什么，见第 10.1 / 10.2 节——**吸附那一步是画面锐利与否的分水岭**，不是可选的精修。

> 这个"整数倍匹配"不是装饰：只按包围盒取最大非黑区域时，会锁到我们自己的悬浮球（1:1 的圆）或天马G 顶部的 FPS 悬浮条。用原生比例做判据能把它们干净地排除。

目标应用切到前台 1.5 秒后自动执行一次（等游戏画出真实帧，避开黑屏）。写配置/还原配置成功后会**清空已锁定的取景窗**——尺寸变了旧的必然错位。

### 备份策略

`RetroArchBackupManager`：`/storage/emulated/0/RetroAIScaler/backups/<时间戳>/config/…` 全量快照，跳过 `.bin/.rom/.idx` 和 >256KB 的文件（config 目录里 29MB 绝大部分是 PS1 BIOS，静态且从不修改）。

- 服务启动时检查，无备份或最新的超过 15 天则新建；最多保留 5 份，**清理只在新建时发生，还原绝不删备份**；
- 还原只动带 `# --- modified by RetroAI-Scaler ---` 标记行的文件（RA 忽略 `#` 开头），避免把用户这半个月的其它调整一起抹掉；

  > **改这个串等于把旧标记变成孤儿。** 项目更名时它被一并改掉，结果是：**旧版本写下的配置从此不被识别，"只还原我们改过的" 悄悄变成 "那个文件永远不还原"**，用户的 RetroArch 会永久停在我们的取景窗设置上。现在 `MARKERS` 是一个列表，**识别接受历史上所有写过的串，写入永远只用最新那个**。以后再改名，往列表里加，不要替换。
- 每个文件走**临时文件 + rename** 原子替换，中途被杀不会留下截断文件；
- 服务停止时自动还原（独立线程，`isDaemon=false`）；启动时若发现残留标记文件说明上次是崩溃，**先还原再备份**——顺序反了会把我们自己的修改当成用户原件存进快照。

---

## 3. 渲染管线

`CaptureBridge`（MediaProjection → ImageReader）→ JNI → `HwBufferReader`（AHardwareBuffer → EGLImage → 外部纹理，零拷贝）→ `GlRenderer` 合成 → 全屏透明 SurfaceView。

### 采样坐标：曾经的头号画质杀手

```glsl
// 错误：把 uv∈[0,1] 映射到纹素中心之间（0.5 → size-0.5）
vec2 texel = (uSourceRect.xy + 0.5 + uv * (uSourceRect.zw - 1.0)) / uCaptureSize;
// 正确：uv 跨越整个矩形，uv=(i+0.5)/size 必须精确落在纹素中心 i+0.5
vec2 pos = clamp(uv * uSourceRect.zw, vec2(0.5), uSourceRect.zw - vec2(0.5));
```

两种约定混用会让采样位置从左到右累计漂移满一个纹素，`nativeTexel(0)` 算出 `0.998` 而非 `0.5`——**每次采样都是相邻两个原生像素的 50/50 混合**。所有引擎拿到的输入都是糊的，边缘重建的邻居比较也建立在混合值上。改任何采样代码前先确认这一点。

### 放大引擎

| 引擎 | 原理 | 延迟 | 适用 |
|---|---|---|---|
| **像素边缘重建**（默认） | Scale2x/AdvMAME2x 规则，改写为任意倍率连续求值 + 象限边界 1 输出像素抗锯齿 | 零 | 2D 像素画 |
| **HD-2D 光影** | 像素边缘重建 + 深度打光 + 移轴景深 + 泛光（第 13 节） | +1 帧（深度） | 想要"现代化"观感 |
| GPU 锐化 | 边缘定向锐化 | 零 | 更硬朗的观感 |
| ESPCN Fast / HQ / Ultra | NCNN Y 通道超分 | +1 帧 | PS1、3D、有渐变和抖动的画面 |

> **HD-2D 是一个引擎预设，不是一个开关。** 光照 75% / 景深 50% / 泛光 25% 这三个值是真机逐档定下来的，选它就是这一套；选任何其它引擎会把三个效果**全部清零**，并且启动时重新套用一次——否则旧配置里存着的"光照开着"会在换引擎后阴魂不散。见 `UpscaleEngine.hd2dStrength` 一族。

**为什么 2D 像素画上小 CNN 打不过比较规则**：像素画不是低分辨率照片，是作者在原生分辨率上画完的成品，**没有"丢失的细节"可恢复**。真正有效的是判断"这串阶梯本该是斜线"并重建它——这是比较问题不是回归问题。ESPCN Fast/HQ 在合成验证集上只比双线性高 1.4~1.7 dB，对像素画不可见。

深度上去之后情况变了（Ultra：48 通道 × 6 层，**+5.9 dB**），说明之前是容量不足而非方法错。但 Ultra 是 3.4 GMAC/帧，只有 ncnn Vulkan 后端才现实。

采样一律用 `sharpUV()`（像素画专用 sharp bilinear：块内纯色、边缘锐利、过渡带压到 1 个输出像素）。网络输出也要按**它自己的网格**（`uNativeRes × uAiScale`）吸附，用普通双线性采会把重建出的锐利细节又抹掉。

> 这个倍率一度被写死成 `3.0`，而 UI 提供 1x/2x/3x/4x——于是**除 3x 外每一档都在按不存在的网格吸附**，把网络刚重建出来的细节又抹平。改采样倍率的代码时，确认它跟着 `aiScale_` 走。

### 异步推理 + 帧对配

推理 11~50ms，远超 16.6ms 帧预算，放在独立线程。但合成时**绝不能拿新的 RGB 配旧的重建亮度**（运动画面会出色边），所以 base RGB 被拷进原生分辨率纹理，和送去推理的那一帧配对，推理完成才整体切换（ping-pong 双缓冲）。代价是 ESPCN 通道慢 1 帧，着色器通道零延迟。

### 输出摆放

在取景窗四周的空白带里找**最大整数倍**：`k = min(带宽/原生宽, 带高/原生高)` 取整，选 k 最大者居中。**必须是整数倍**——分数倍（早期"全屏等比"给出 3.975x）意味着一个游戏像素落不到整数个输出像素上，每条像素边界都被插值，画面就是软的。全部按屏幕实时算，不写死。

### CRT 遮罩

**必须在线性光照下相乘**（`pow(2.2)` → 乘 mask → `pow(1/2.2)`）。sRGB 是伽马编码的，直接相乘会让中间调压暗得远超物理应有的程度，这是早期"一开遮罩就发闷"的根本原因。

遮罩按**游戏像素**索引：**一个游戏像素一个三元组**，磷光带用面积覆盖率加权（`bandCover()`），不是硬三等分。

> **这条曾经是反的，理由听起来也对，但它错了。** 原实现按屏幕像素索引、三元组硬编码 3 px，理由是"真实荫罩的间距固定在玻璃上"。对 CRT 是对的，对这里是错的，错在**混叠**：画面按整数倍 k 绘制，一个游戏像素占 k 个屏幕像素，**k 不是 3 的倍数时相邻游戏像素落在三元组的不同相位上**——k=4 时依次是 `[R,G,B,R]`、`[G,B,R,G]`、`[B,R,G,B]`，每个游戏像素被染上一种原色，周期 3 个像素循环。实测通道失衡：25% 强度下 **12%**，50% 下 **29%**。这不是"看不见"，是一层彩色爬行。
>
> **它藏了这么久，是因为遮罩是在 GBA + 1080p 上调的，那里 k=6**——整个矩阵里仅有的两个"3 整除 k"的格子之一。其余组合（FC/SFC/MD/PS1 普遍 k=4，GBC 在 1080p 上 k=7）全都在爬。
>
> 当初拒绝按游戏像素索引的理由是"一个三元组需要 ≥6 个输出像素才画得下，4x 时不可能"。**那只对硬三等分成立**；面积加权没有这个限制，k<3 时它退化成一层无色的均匀压暗——那是诚实的答案，不是混叠出来的猜测。数值验证：k=1~8 三通道失衡恒为 0，平均亮度与旧实现完全相同。
>
> **代价是自愿付的**：遮罩粗细现在跟着平台和倍率变，真实荫罩不会。静态的粗细变化比动态的彩色爬行好受。

同理，荫罩的纵向明暗也按**游戏行**的半周期做面积加权。固定屏幕行周期（原来是 2 px）在 k 为奇数时会以同样的方式拍频，只不过表现为亮度条纹而不是彩色条纹。

三种几何取自 Lottes 的 `dotmask.slang`（Unlicense/公有领域）：光栅 Aperture（3px 竖条）、荫罩 Shadow（6×2 交错）、狭缝 Slot（沿 Y 错切）。扫描线用高斯束流剖面并按 `σ√(2π)` 归一化，保证全白场亮度不变。

---

## 4. 线程与生命周期（崩溃全出在这里）

**EGL context 同一时刻只能绑一个线程。**

- 渲染在 capture 线程，但主线程也会调 `clearOverlay()`（暂停/看门狗/销毁）。`clearOverlay()` 末尾**必须解绑** context，否则 capture 线程之后每帧 `EGL_BAD_ACCESS`，表现为"恢复后再也不出画面"。
- capture 线程退出前必须调 `nativeDetachEglContext()`，否则 context 留在死线程上，主线程的清理全部失败。

**推理线程持有 ncnn::Net，任何释放前必须先 `stopAiWorker()`。**

`loadEspcnModel()` 做了，早期 `unloadEspcn()` 漏了 → 主线程 `delete ncnn::Net` 时推理线程正在 `processLuminance` 里 → `pthread_mutex_lock called on a destroyed mutex` → SIGABRT。Fast/HQ 因窗口窄不易触发，Ultra（单次 50ms+）几乎必中，表现为"从 Ultra 切走就崩溃"。

**菜单窗口不能压在取景窗上**：菜单也会被 MediaProjection 录进去，哪怕只重叠几像素也会被采样放大，看起来像画面里有菜单的鬼影。`positionMenuAwayFromSource()` 把菜单停到取景窗对角。

---

## 5. 模型训练

**训练脚本已移到模型仓库**（`RetroAI Model/scripts/train_espcn.py`）——那边才是放模型的地方，这边只消费 assets。用 `--out` 直接写进本仓库的 assets：

```bash
cd "/path/to/RetroAI Model"
.venv/bin/python scripts/train_espcn.py --scales 6 \
    --out "/path/to/RA AI Render/app/src/main/assets/models"
```

本仓库默认**没有** `.venv`；模型仓库（`RetroAI Model/.venv`）里那个依赖完全一样（torch / numpy / pillow），可以直接借用：

```bash
"/path/to/RetroAI Model/.venv/bin/python" scripts/train_espcn.py --scales 6
```

**每个倍率是独立模型**，补一档不需要重训其它档。ESPCN 和深度网络在 Mac 上**都**走 MPS 就够——`train_depth.py --device` 接受 `mps` 且 auto 会自己选中它，它只拒绝纯 CPU。（这里原先写着"深度网络坚持要 CUDA"，**不对**；深度网络 200 轮在 M 系列上约 55 分钟。）

产物直接落到 `app/src/main/assets/models/`，**直接写 ncnn `.param`/`.bin`，不经过 onnx2ncnn**：

```
.bin 每个 Convolution = [uint32 tag=0][weights f32][bias f32]
weights 布局 [out][in][kh][kw]，与 PyTorch 一致
ReLU 折进卷积（param 9=1），比独立层快
```

**训练对的构造是关键**：LR **不是** HR 的模糊降采样，而是同一套矢量场景在 1x 下的**独立渲染**。游戏原生帧本身就是原分辨率作画，不是照片降采样；用错配对会让网络学成过锐化。另混入 30% 硬边像素画样本，避免抹平精灵边缘。

通道数**必须是 8 的倍数**：ncnn 的 fp16 packing 布局否则退化到标量路径——实测 12/6 的网络跑 22.9ms，而大得多的 32/16 只要 13.8ms。

**倍率档位**：2/3/4/**6**。6x 是因为它就是真机实际绘制的倍数——240×160 在 1920×1080 上能容下的最大整数倍是 6，低于它网络重建完还要再放大一次，高于它多算的细节会在缩小时被丢掉。**深度网络不受影响**：它 native→native，压根没有倍率这个参数。

> 倍率跟屏幕和主机绑定：6x 只对「1080p + GBA」成立。真要做干净，应该让倍率**跟着实测几何自动选**，而不是让玩家猜——这是个更根本的改动，还没做。

| 变体 | 结构 | MAC/px | 3x PSNR（合成集） |
|---|---|---|---|
| fast | 16ch × 1 隐藏层 | 2200 | 27.09（双线性 25.70） |
| hq | 32ch × 1 | 6704 | 27.38 |
| ultra | 48ch × 4 | 88032 | **31.45** |

**倍率实测（Pocket FIT Elite / Adreno 840）**：Ultra 从 4x 的 8.0ms 到 6x 的 9.5ms，只多 1.5ms——虽然算术量是 2.25 倍。固定开销占大头，所以**倍率涨得比耗时快**。6x 值得用，因为它和输出几何点对点。

---

## 6. 构建

```bash
./setup_toolchain.sh                    # SDK/NDK/CMake/ncnn 预编译库
JAVA_HOME=<jdk17+> ./gradlew assembleDebug
```

> **Homebrew 的 `openjdk` 是 keg-only，装了也不在 PATH 上。** `/Library/Java/JavaVirtualMachines/` 是空的时候，`/usr/bin/java` 和 `/usr/bin/keytool` 这两个苹果转发壳会报 "Unable to locate a Java Runtime"——JDK 明明装着。一次性修好：
>
> ```bash
> sudo ln -sfn /opt/homebrew/opt/openjdk/libexec/openjdk.jdk \
>     /Library/Java/JavaVirtualMachines/openjdk.jdk
> ```
>
> 之后 `keytool`、`/usr/libexec/java_home` 和 Gradle 都能自己找到它，不用再显式带 `JAVA_HOME=`。**本机是 JDK 22，CI 是 Temurin 17**，项目 target 17；到目前为止没出过问题，但"本机过 CI 不过"这类怪事第一个该怀疑它。

**三项检查在构建期跑**（都已接进 Gradle）：

- `tools/check_shaders.py` —— 编译内嵌的 GLSL；
- `tools/check_shading_parity.py` —— 把 `depth_profile.cpp` 编到宿主机上和模型仓库的 numpy 逐像素对拍，并逐个比对光照常数。**HD-2D 的离线评估和真机跑的必须是同一个东西**，13.5 / 13.11 / 13.6 三次都栽在这上面；
- `tools/check_scroll_estimator.py` —— 滚动估计器能否复原已知位移，**含符号**（13.3 记着上一次运动补偿是符号反了还被测了两轮才发现）。

后两个会在系统 `python3` 没有 numpy 时**自动 re-exec 到有 numpy 的解释器**。原先它们直接打印 "skip" 然后 exit 0——**一个不会失败却声称验证过的门，比没有门更糟**。模型仓库不在旁边时才真正跳过。

> **改 `.so` 之后覆盖安装前要先停服务。** 服务还在跑时 `install -r`，旧进程会带着对不上的 native 库继续跑，实测表现为**整块增强画面变黑**——这不是代码问题，`adb shell am force-stop com.retroai.scaler` 或强制重装即可。

着色器那一项：一个编不过的着色器**不会崩溃**——渲染器按第 1 节的安全底线退化为全透明，于是它表现为"增强画面凭空消失"、其余一切正常，logcat 里只有一行证据。这已经耗掉过一次真机往返。

> **构建失败时绝不能装旧包。** `check_shaders.py >/dev/null && ./gradlew … ; adb install …` 这种写法吞掉了失败信息、`&&` 正确地跳过了构建，然后 `;` 后面的 install 照样把上一次的 APK 推了上去——连续两次改动被报告成"已安装"，而机器上跑的是旧代码，表现为"新功能没有任何效果"。**install 必须跟在构建成功之后。**

`app/src/main/cpp/libs/ncnn/` 是 77MB 预编译库，已 gitignore，由 `setup_toolchain.sh` 下载。ncnn 编译时带 Vulkan（`NCNN_VULKAN 1`），CMake 链接 `libvulkan`；加载模型时 `get_gpu_count()` 探测，有 GPU 走 Vulkan，否则退回 CPU。

---

## 7. 已知限制

- **取景窗占掉屏幕一角**，安卓 11 MediaProjection 架构的硬约束。API 34 的单应用捕获可彻底消除；平台签名 + `READ_FRAME_BUFFER` 走 `ScreenCapture.captureLayers` 只截 RA 图层也可以，但需要 OEM 配合。
- **虚拟显示路线已否**（2026-08-20，Air Mini / Android 11 实测）：shell 创建的虚拟屏没有 `FLAG_TRUSTED`，活动被静默重定向回 display 0；Shizuku 能授的权限上限就是 shell，接了也一样。详见 11.7。
- AYANEO Air Mini 固件里「系统设置 → Root 脚本」实测**无法执行 .sh**（点运行后一直转圈，脚本从未运行），root 路径在该机型上不可用。
- ESPCN 通道有 1 帧额外延迟；对操作延迟敏感时用像素边缘重建（零延迟）。
- **HD-2D 的光影仍比画面晚约半个像素，60Hz 上下摆动，实测"几乎感受不到"。** 这是重投影补偿之后的地板，见 13.12。
- 自动裁切（`frame_cropper.h/.cpp`）代码在但**未接入管线**，取景窗位置由第 2 节的探测流程负责。
- 仅打包 arm64-v8a。
- RA 主配置无法读写（安卓限制），因此不在备份范围内——本工具也从不修改它。
- **从多任务界面点模拟器自己的卡片返回时，增强画面会消失**，直到切一次别的应用再回来。详见第 10.3 节：SurfaceView 的缓冲队列被该转场弄丢，而应用侧收不到任何通知。

HD-2D 自己的已知限制（都在第 13 节有数据）：

- **圆角气泡框**检测不到——面板检测器要的是闭合直角边框。
- **不带框、直接画在场景上的浮动文字**（"LEVEL UP!" 那类）仍会被打光。
- **有细节的视差远景层**会被"屏幕锁定"检测器误判成 HUD。因此那个通道**只驱动景深**，不碰光照——在天空上少一点虚化，比在光照上挖个洞便宜得多。
- **深度抖动与光影滞后是一条一维权衡**，没有免费午餐（13.3③ 记了四条实测失败的破局思路）。当前坐在中点。
- GBA 开机 logo 上的光斑**保留为特性**。

---

## 8. 第三方来源

- **ncnn** — Tencent，BSD 3-Clause，预编译库经 `setup_toolchain.sh` 获取。
- **CRT 遮罩几何** — 思路取自 Timothy Lottes 的 `dotmask.slang` / `energy_conservation_mask.h`（Unlicense / 公有领域），本项目为独立实现。
- **Scale2x / AdvMAME2x** — Andrea Mazzoleni 提出的算法规则，本项目为任意倍率的独立实现。
- 参考用的 RetroArch slang-shaders 仓库（`crt/`）已 gitignore，不随本项目分发。

---

## 9. 后续待办

**9.3 已完成**（ESPCN 补到 2/3/4/6 四档，见第 5 节；重绘路线在 12.2 被否掉，代码已从模型仓库删除）。剩下两项，按可以独立开工的顺序写。

### 9.1 Release 包签名（已完成）

日常构建仍出 debug 包，用仓库里固定的 `debug.keystore`（别名 `Sanker`，口令 `android`）。它被提交是**有意为之**——本机和 CI 共用一个签名，包才能互相覆盖安装；换签名会导致 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`，必须卸载重装。

**打 `v*` tag 才走 release 签名**，密钥从 GitHub secret 注入，落到 runner 的临时目录，构建后无论成败都删（`if: always()`）。

**没有回退路径，这是关键。** `signingConfigs.create("release")` 只从环境变量读，读不到就留空；`gradle.taskGraph.whenReady` 里再查一遍四个变量，缺任何一个直接 `GradleException`。实测无凭据时 **0.66 秒失败，编译都不开始**。

> 检查必须挂在**任务图**上，不能写成 `assembleRelease` 的 `doFirst`——那是个生命周期任务，它自己的 `doFirst` 在打包**之后**才跑，APK 早就出来了。
>
> 为什么宁可炸也不回退：**悄悄用 debug 密钥签的正式包，看起来正常、装得上、跑得动**，只是它的私钥公开在这个仓库里。而密钥一旦丢失或换掉，已安装的用户就永远收不到更新了——签名对不上，只能卸载重装、存档全失。这个不对称性决定了这里只能大声失败。

本机验证签名（需要自己的 keystore，口令走 `read -rs` 不落 shell 历史）：

```bash
export SIGNING_KEYSTORE_FILE=~/Documents/release.jks
export SIGNING_KEY_ALIAS=Sanker
read -rs SIGNING_STORE_PASSWORD && export SIGNING_STORE_PASSWORD
export SIGNING_KEY_PASSWORD="$SIGNING_STORE_PASSWORD"
./gradlew assembleRelease
$ANDROID_HOME/build-tools/34.0.0/apksigner verify --print-certs \
    app/build/outputs/apk/release/app-release.apk
```

**R8 仍然关着**（`isMinifyEnabled = false`）。要开得先验证 JNI 入口（`Java_com_retroai_scaler_jni_NativeBridge_*`）和 ncnn 的反射路径不被裁掉——那是独立的一轮真机往返，不该和首次签名挤在一起。

密钥怎么生成（**口令只有持有者知道，不进任何文档、任何日志**）：

```bash
keytool -genkeypair -v -keystore release.jks -storetype PKCS12 \
  -alias Sanker -keyalg RSA -keysize 2048 -validity 10950
base64 -i release.jks | pbcopy      # 存进 GitHub Secret
```

需要的 Secrets：`SIGNING_KEYSTORE_BASE64`、`SIGNING_STORE_PASSWORD`、`SIGNING_KEY_ALIAS`、`SIGNING_KEY_PASSWORD`。

**密钥丢失 = 永久无法给已安装用户推更新。** 生成后立刻离线备份，这一条没有补救办法。

### 9.2 更多遮罩类型

当前三种在 `gl_renderer.cpp` 的合成着色器末尾，由 `uMaskType` 选择（0 关 / 1 光栅 / 2 荫罩 / 3 狭缝），枚举在 `OutputProfileManager.kt` 的 `MaskType`，UI 是 `chipGroupMask`。

新增遮罩必须遵守三条既有约束（都是踩坑换来的，见第 3 节）：

1. **按屏幕像素索引**（`floor(px)`），不是游戏像素——真实荫罩间距固定在玻璃上；
2. **在线性光照下相乘**，已在外层做了 `pow(2.2)` / `pow(1/2.2)` 包裹，新遮罩只需返回 mask 向量；
3. **能量守恒**——用 `light`/`dark` 一对系数并让均值接近 1，否则一开就发暗。

可参考的候选（`crt/` 是 RetroArch slang-shaders 的克隆，已 gitignore，本地保留）：

- `crt/crt-effects/shaders/dotmask.slang` — Lottes 的 5 种变体，其中 `shadowMask==5` 的 `SinPhosphor` 和 cgwg 的品红/绿交替还没实现；
- `crt/crt-effects/shaders/energy_conservation_mask.h` — 更严谨的亮度守恒推导；
- `crt/shaders/crt-beans/mask.inc`、`crt/shaders/**/slotmask.slang` — 带间距参数的狭缝栅。

~~值得一并加的：**遮罩间距可调**（现在硬编码 3 屏幕像素）~~ —— **已作废**。间距不再是屏幕像素，而是一个游戏像素一个三元组（第 3 节），k=6 时正好给出这条待办想要的 6px 观感，其余倍率自动跟着走。真要再加参数，应该是"几个游戏像素一个三元组"，不是屏幕像素数。

### 9.3 更多 / 更大的 AI 模型（已完成，保留结论）

三个变体在**模型仓库**的 `scripts/train_espcn.py` 里（`VARIANTS`），结构是「5×5 头 + N 个 3×3 隐藏层 + 亚像素尾」，纯前馈无跳连。倍率已补齐到 2/3/4/6。

| 变体 | 结构 | MAC/px | 3x PSNR |
|---|---|---|---|
| fast | 16ch × 1 | 2200 | 27.09 |
| hq | 32ch × 1 | 6704 | 27.38 |
| ultra | 48ch × 4 | 88032 | 31.45 |

（双线性基线 25.70。Ultra 在 Mali-G57 上走 Vulkan 约 20fps AI 刷新率、GPU 占用 84%；旗舰 GPU 上应该是个位数毫秒。）

扩展方向，按性价比：

1. **残差块**。现在是纯前馈，深度再加会难训。加跳连需要导出器支持 `Split` + `Eltwise`——**这个障碍已经解决了**：模型仓库的 `retroai/ncnn_export.py` 支持这两层（RetroDepth 的 5 个残差块就靠它），照抄即可。
2. **RGB 三通道**而非仅 Y。色度目前是从同帧双线性采的，色彩过渡多的画面（PS1）可能吃亏。代价是算力 ×3 和输入输出改造。
3. ~~**蒸馏**。用 Real-ESRGAN 之类的重型模型生成目标~~ ——**已试过并否掉**，见 12.2。那条路在像素画上是结构性失败，代码已从模型仓库删除。
4. **更好的训练数据**。现在是程序生成的矢量场景 + 30% 硬边像素画。真实游戏截图（多平台、多风格）大概率比合成数据更有效。

不变的约束：

- **通道数必须是 8 的倍数**，否则 ncnn 的 fp16 packing 退化到标量路径（实测 12/6 比 32/16 还慢）；
- 导出的 `.param`/`.bin` 要能被 `EspcnInference::loadModel` 直接读，字节数必须精确匹配（每个 Convolution = `[uint32 tag=0][weights f32][bias f32]`）；
- 新增变体记得同步 `UpscaleEngine` 枚举、`chipGroupEngine` 的 chip、以及 `modelAssetBaseName()` 的命名规则 `espcn_y_<倍率>x_<变体>`；新增**倍率**还要同步 `AiScale` 枚举和 `chipGroupScale` 的 chip。

---

## 10. 跨机型兼容性（2026-08 实测）

第二台实测机：**AYANEO Pocket FIT Elite** — 骁龙8 Elite（SM8750P）/ Adreno 840 / Android 16 (API 36) / 1080×1920 竖向原生面板横用为 1920×1080 / 144·120·90·60Hz。

相对 Air Mini（Helio G95 / Android 11 / 1280×960 横向原生 / 60Hz），SoC 厂商、GPU 架构、系统版本、面板方向、刷新率全部不同。

**这轮暴露出来的缺陷绝大多数不是"新机型不兼容"，而是本来就存在的竞态和错误假设，只是 Air Mini 的参数恰好一直站在幸运的一侧。** 所以下面每一条对 Air Mini 同样是净收益。

### 10.1 取景窗必须吸附到原生分辨率的精确整数倍

**这是发糊问题的唯一根因，优先级高于任何放大算法。**

探测在 320×240 缩略图上做连通域，1080p 下 `sy = 4.5`——一个检测行等于 4.5 个屏幕像素。边界框差半行，量出来就是 `240×162` 而不是 `240×160`。

两个像素看着无害，实际是致命的。合成着色器 `sampleSourceSharp()` 先按 `uNativeRes`（160）吸附 uv，再乘 `uSourceRect.zw`（162）：

| 原生行 i | 应采样 | 实采样 | 偏差 |
|---|---|---|---|
| 0 | 0.5 | 0.506 | ~0 |
| 80 | 80.5 | 81.51 | 1 行 |
| 159 | 159.5 | 161.49 | 2 行 |

每次采样都落在两行之间，且偏差从上到下累积——**所有引擎共用这个输入，所以像素边缘重建和 ESPCN 会一起糊**。判断"是不是取景窗坐标问题"的最快方法就是看两个引擎是否同时糊。

因此 `runDetectionPass()` **不使用实测边界框**，而是吸附到匹配到的 `native × k`，位置取实测中心。**位置故意不吸附**：偏 1px 只是整体平移，肉眼不可见；尺寸非整数倍才会让每个像素都被重采样。

### 10.2 探测的判据是长宽比，不是尺寸误差

原先把宽高误差相加、阈值 35%，对**比例失真**几乎没有鉴别力，于是放行了两类假阳性：

- RA 停在菜单（未载入核心）时全屏绘制，1920×1080 被判成"8 倍的 240×160，差 16%"
- 旋转后 RA 重排成 243×136，被判成"1 倍，差 16%"

长宽比与尺度无关，能干净分开：真实取景窗与原生比例差 ~1%，上面两个都差 ~19%。所以先过 8% 的长宽比闸门，再做整数倍匹配。

宁可拒绝也不能放行——比例失真的矩形会让画面**全局发糊**，比不显示更糟。

### 10.3 线程与 EGL：三条硬约束

**（a）全局管线锁绝不能在主线程上为廉价读取而获取。**
`nativeProcessHardwareBuffer` 持锁跨越整个 GPU 帧（EGL 绑定 + EGLImage + renderFrame + swap）。HUD 每 500ms 在主线程拿同一把锁只为拷 5 个 float，在 120Hz 下直接饿死主线程 → ANR → 连带整条 ESPCN 功能线不可达。性能计数器现在是文件作用域的 atomic，读取端**不碰锁也不解引用 `gRenderer`**（后者是关键，否则无锁读会和 `nativeRelease` 竞争）。

**（b）GL 操作必须在 EGL context 所在的线程上执行，不要把 context 抢过来。**
context 常驻捕获线程且帧间不解绑。暂停时的 `clearOverlay()` 若从主线程调用，`ensureEglContextCurrent()` 会静默失败、擦除变成空操作，最后一帧永久冻结在屏上（表现为"整机卡死"，但触摸其实是通的）。60Hz 下主线程偶尔能抢到，120Hz 下几乎抢不到——典型的时序依赖。正确做法是 `CaptureBridge.runOnCaptureThread { }` 到 context 所在处执行。

**（c）ncnn 模型加载必须在工作线程。**
首次调用 `get_gpu_count()` 会拉起整个 Vulkan 栈；`unloadEspcn()` 要 join 推理线程（Ultra 单次 50ms+）。任一放在主线程都会和（a）叠加成 ANR。

### 10.4 覆盖层窗口的不透明度会拦截其它应用的触摸

Android 12+ 的不可信触摸规则：**跨 UID** 的窗口不透明度超限就拦截穿透触摸。系统会把 `FLAG_NOT_TOUCHABLE` 的覆盖层钳到 0.8 并打日志说"以便让触摸穿透"，但**这台机器在 0.8 上仍然拦截**。

行为特征（日志里可能什么都不打，别指望 `untrusted` 字样）：
- 自己应用的窗口能点（同 UID）
- 启动器、多任务点不动（跨 UID）
- 移除覆盖层立刻恢复

暂停时把窗口 alpha 降到 0.5 可释放触摸。**但绝不能降到 0**：完全透明的窗口会停止合成，之后恢复 alpha 属性也不会让 SurfaceView 的子图层重新接上，渲染器会对着一个不再上屏的 surface 继续"成功"绘制。

### 10.5 旋转

`screenWidth/Height` 原先在 `onCreate()` 锁死且全项目无旋转处理。竖向原生面板上这个问题被放大。现在由 `surfaceChanged` 驱动重建，两个要点：

- **去抖 350ms**。切多任务会让窗口尺寸抖一下再回来（1920×1080 → 1920×1024 → 1920×1080），立即响应等于每次切应用都白拆重建两遍管线。
- **`VirtualDisplay.resize()`，不要 release 后重建**。同一个 MediaProjection 上重复 `createVirtualDisplay()` 的行为跨版本不可靠。ImageReader 不能 resize，新建后再换，旧的等新表面挂上去才关。

重建后必须重推所有设置（渲染器是新对象，ESPCN 权重也要重新加载），并清空已探测的取景窗——它是旧方向的坐标。

### 10.6 前台检测必须处理 ACTIVITY_PAUSED

只跟 `ACTIVITY_RESUMED` 时，多任务界面（SystemUI，不发新的 RESUMED）会让判定停留在"模拟器仍在前台"，覆盖层继续绘制一张**已停止更新的全屏静止画面**——看起来就是死机。目标应用一暂停就清空前台判定。

注意轮询周期 400ms 仍可能**整个错过**一次快速的多任务往返，不要把任何关键逻辑挂在这个转场上。

### 10.7 已知未解决：多任务返回导致 surface 断连

从多任务点模拟器**自己的卡片**返回时，SurfaceView 的缓冲队列被弄丢：

- SurfaceFlinger 的 `frame=` 计数**冻结**（`dumpsys SurfaceFlinger | grep -A2 "SurfaceView\[\](BLAST)"`），缓冲区句柄不变
- 应用侧 `eglSwapBuffers` 全部返回成功，`success=1 paused=0`，几何有效
- **`surfaceDestroyed` 从不回调**，应用完全无从知晓

试过两个触发点都不可靠：前台转场经常根本观察不到（快过 400ms 轮询）；转场产生的尺寸抖动只是**有时**发生。`recreateOverlaySurface()` 保留在代码里但没有调用点。

**当前绕过方式：切一次别的应用再回来。** 走任何其它应用都会自然重建窗口，所以那条路径一直正常。

> 诊断教训：`renderFrame()` 在暂停时也返回 `true`（画一帧透明就什么都不做），所以 `success=1` 长期是个**在异常路径上返回成功的假信号**，比没有指标更糟。帧日志现在带 `paused=`。凡是"管线全绿但屏幕上没有"的情况，直接去 SurfaceFlinger 看 `frame=` 是否在涨——那是唯一不会骗人的指标。

---

## 11. 单应用捕获（API 34+）

安卓 14 起，录屏授权对话框会让用户在「整个屏幕」和「一个应用」之间选。选后者时**我们自己画的覆盖层不在捕获里**，第 1 节那条"取景区和输出区不能重叠"的硬约束就不存在了。

实测确认（Pocket FIT Elite / Android 16）：单应用模式下覆盖层完全不进捕获帧，探测标记的差分为 **0.00**；系统那圈"正在共享"的白框也不进。

### 11.1 它解决什么，不解决什么

**解决**：取景窗从用户视野里消失——不是不存在，是被增强画面精确盖住。

**不解决**：超分输入仍然必须是**原生分辨率的 1:1 像素**。捕获到的是 RA 整个窗口的表面（1920×1080，和整屏模式一模一样），里面只有一小块是游戏。所以 RA 仍然要被配置成画 1x，取景窗探测和整数倍吸附一条都不能少。

### 11.2 模式判定：探测，不要问 API

`MediaProjection.Callback.onCapturedContentResize` / `onCapturedContentVisibilityChanged` 看起来是天然的判据，**实测两种模式下都触发，且都报 1920×1080**，无法区分。

改成直接测我们关心的性质本身：屏幕左上角画一块洋红标记 → 空 4 帧 → 回读捕获帧那个角落 → 擦掉 → 再回读 → 比较。**差分**判据（而不是绝对色值）让底下画的是什么都无所谓。

实测裕度极大，阈值不需要调：

| | delta | markerDist |
|---|---|---|
| 单应用 | 0.00 | 1.16 |
| 整屏 | 0.93 | 0.23 |
| 阈值 | >0.25 | <0.35 |

没有 API 能强制单应用模式（只能强制整屏），所以这个探测是必需的，不是优化。

### 11.3 时序上的死结：配置必须先写，模式只能后测

RA 的覆盖配置**只在载入内容时读取**，必须在 RA 启动前写好；而捕获模式**要等出画后才测得到**。这个先后关系无法调换。

于是首次写入永远是**预测**：按 API 版本给初值，并把实测结果存进 SharedPreferences 作为下次的预测。实测与预测不符时重写配置并要求重启 RA——只有第一次会遇到，之后总是对的。

**等待期必须说人话。** 这段时间画面是空白的（RA 还按旧布局在画，采样全错，画出来是乱的）。最初只有一个 Toast，实测反馈是"完蛋，黑屏了"——**空白 + 几秒就消失的提示，等于崩溃**。现在改成常驻横幅，条件消失才撤。

> **清理路径不要加条件。** 那个横幅第一版撤不掉：移除前判断了 `isAttachedToWindow`，而 `addView` 到第一次 traversal 之间它仍是 `false`，落在这个窗口期就会**跳过 removeView 却把引用清空**，横幅变成孤儿留在屏幕上，之后每次调用都在"引用为空 ⇒ 无事可做"上直接返回，连服务销毁都撤不掉它。
>
> 这和 10.4 那个 alpha 缓存短路是同一类错误：**本地记录与真实状态不同步，而代码信了记录**。凡是清理/恢复路径，直接执行并用异常兜底，不要先判断"是不是已经不需要做了"。

### 11.4 `uProtectSource`：一行 discard 的两副面孔

合成着色器里这行在整屏模式下是**防自激的生命线**，在单应用模式下是**画面中间的一个洞**：

```glsl
if (uProtectSource != 0 && inside(px, uSourceRect)) discard;
```

改几何时漏掉它，表现为"原生小窗盖在增强画面上面"——其实是我们自己把那块挖空了。它跟着捕获模式走，并且打进 `Geometry:` 日志的 `protect=` 字段，方便一眼核对。

**判错模式的代价不对称**：单应用判成整屏，只是中间多个洞；整屏判成单应用，会立刻自激。

### 11.5 几何：两边都居中

RA 写 `video_viewport_bias_x/y = 0.5`，输出取全屏最大整数倍、**按屏幕居中**。

输出是原生的整数倍、取景窗是 1 倍，同心 ⇒ 覆盖由几何自动保证，不需要任何避让计算。居中还消掉了一个老问题：bias_y=0 到底是上还是下**取决于视频驱动**，而中心在哪种约定下都是中心。

> 注意别按**取景窗**中心对齐：RA 是在自己的窗口里居中的，而系统栏让那个窗口比屏幕矮（1920×1024），中心差 28px，照着对会让上下边框肉眼可见地不等。按屏幕居中，取景窗那么小怎么都能盖住。

### 11.6 满不透明度：把 `FLAG_NOT_TOUCHABLE` 变成动态的

带 `FLAG_NOT_TOUCHABLE` 的覆盖层会被系统钳到 alpha 0.8（见 10.4）。剩下的 20% 让背后 RA 的原生画面**透出来形成残影**——屏幕其它地方背后是黑底看不出来，只有取景窗那一块显形。

**渲染时去掉这个标志、暂停时加回**。渲染时本来就已经拦截跨 UID 触摸了（0.8 就在阈值上），去掉它不额外损失任何东西，却拿回 100% 不透明度；而真正需要触摸的时刻（回到桌面）是暂停态，标志和低 alpha 一起恢复。

改这里务必回归测"回桌面能不能点图标"——`updateViewLayout` 更新窗口属性的可靠性在本项目里有前科（见 10.7）。

### 11.7 虚拟显示路线：已否（2026-08-20，Air Mini / Android 11 实测）

设想是绕开第 1 节：让 RA 跑在我们自建的 `VirtualDisplay` 上，物理屏整块留给输出。真通了收益远不止"取景窗消失"——虚拟屏尺寸由我们定，RA 全屏画 240×160 就是 1:1 原生像素，**第 2 节的配置改写、取景窗探测、整数倍吸附、11.2 的模式判定全部作废**，而且在所有安卓版本上同时成立。

**结论：非受信显示承载不了别的应用的活动，这条路在 root 之外没有入口。**

用 scrcpy 3.x 的 `--new-display` 验证（它同样以 shell 身份创建虚拟屏，权限等级和我们能拿到的一样）：

```bash
scrcpy --new-display=1280x960/320 --no-audio               # 终端 A，占住不放
adb shell dumpsys display | grep 'DisplayInfo{"scrcpy"'    # 拿 displayId，每次重启都变
adb shell am start -S --display <id> -n com.android.settings/.Settings
adb shell dumpsys activity activities | grep -E "Display #|ResumedActivity"
```

对照组用**系统设置**（标准系统应用，一定支持副屏）——它都过不去，RA 更不必试。

| | 内置屏 (0) | scrcpy 虚拟屏 (71) |
|---|---|---|
| flags | `FLAG_DEFAULT_DISPLAY` `FLAG_SECURE` `FLAG_SUPPORTS_PROTECTED_BUFFERS` **`FLAG_TRUSTED`** | `FLAG_ROTATES_WITH_CONTENT` `FLAG_PRESENTATION` `FLAG_OWN_CONTENT_ONLY` |
| owner | — | `com.android.shell (uid 2000)` |

`am start --display` **不抛异常**，但活动被静默重定向回 display 0：`dumpsys activity activities` 里只有 `Display #0`，虚拟屏那个段落根本不出现。根因是虚拟屏缺 `FLAG_TRUSTED`，而 `ADD_TRUSTED_DISPLAY` 是 signature 级权限。

**Shizuku 一并否掉。** scrcpy 这次就是以 shell 跑的，而 Shizuku 能授的上限正是 shell——**这次失败已经是"接了 Shizuku 之后"的权限等级**。剩下只有 root（Air Mini 上不可用，见第 7 节）和平台签名。

> **两个把人绕进去的假阴性，都不是权限问题，单独记：**
>
> 1. **虚拟屏一开始是全黑的。** 它没有 `FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS`，系统不往上面放桌面，OEM launcher 也不声明支持副屏。**那块屏是空的，不是坏的**——停在这里就会把结论记成"虚拟屏出不了画"，完全错。
> 2. **`am start` 漏了 `-S` 时**会打印 `Warning: Activity not started, its current task has been brought to the front`：目标应用已有 task，就被原地提到前台，`--display` 被完全忽略。命令返回成功、无报错、屏幕照旧全黑——**看返回码等于什么都没测**。
>
> 判据只能是 `dumpsys activity activities` 里活动落在哪个 `Display #` 下。这和第 6 节那条 `; adb install` 吞掉构建失败、以及 `check_*.py` 打印 skip 却 exit 0 是同一类东西：**一个不会失败却什么也没验证的步骤**。

第三关这次没走到，但记下来免得以后有人只验前两关就宣布成功：**即使活动能落到虚拟屏上，物理手柄的按键仍未必进得去**。key event 走的是 top focused display 上的焦点窗口，判据是 `dumpsys input | grep -i FocusedDisplay`，而且**必须在掌机上按物理按键验**——scrcpy 是带着 targetDisplayId 从外部注入的，在它窗口里按键一定"能用"，那是假阳性。

### 11.8 整屏模式下让取景窗"看不见"：压暗 + 数值反解（提案，未验证）

**动机**：3.5 寸机型（Pocket Micro / 960×640）上取景窗无法忍受。那块屏和 GBA 同为 3:2，本来 4x 正好铺满；放一个 1x 取景窗之后输出只能退到 3x = 720×480，丢掉 44% 面积，还多一个小画面在角上。1280×960 和竖屏 Elite 上这个代价被富余空间吸收掉了，3.5 寸没有地方藏。

关键认识：**自激的条件是"我们在那块画的东西依赖于我们从那块读到的东西"。**画一层**常数**黑色不构成回路。所以 11.4 那行可以从"挖空"改成"压暗"：

```glsl
// uSourceDim: 0.0 = 现行行为（逐位等价），0.75 = 只透 25%
if (uProtectSource != 0 && inside(px, uSourceRect)) {
    if (uSourceDim <= 0.0) discard;
    outColor = vec4(0.0, 0.0, 0.0, uSourceDim);
    return;
}
```

采样侧（`Y_EXTRACT`）把取回的值除以 `1 - uSourceDim`。代价是量化精度：α=0.75 掉 2 bit（±0.5 的舍入被放大成 ±2/255），α=0.5 掉 1 bit。

**保留 `discard` 分支不是可选的**：`uSourceDim = 0` 必须逐位等于今天的行为，这样改动天然可回退，出问题能单变量排除。

三件必须做的事：

1. **别假设 blend 是线性的。** SurfaceFlinger 在哪个空间合成不保证。在压暗层下显示 0~255 灰阶、回读、建逐通道 256→256 的 LUT 来反解。12.7 那条"语料颜色不落在 RGB555 网格上"的悬案就在这条链路上，正好顺手测出来。
2. **判据是逐位相等，不是"看起来差不多"。** 静止画面（RA 暂停）下先按 `uSourceDim=0` 捕一帧当 ground truth，再开压暗捕一帧反解，逐像素比。语料是在无压暗路径上采的、模型跟那条路径绑死（12.7），有残差就是分布漂移，得量出来才知道能不能接受。
3. **先试 α=0.5**（只掉 1 bit）看观感够不够，不够再往上推。

**会杀死这个方案的是合成时的 dithering**——它把确定性舍入变成空间噪声，反解就不可能精确。第 2 条一跑就知道，不用猜。

> 零成本的替代：捕获发生在 SurfaceFlinger 里，**不在玻璃上**。往取景窗那个角贴一小片哑光贴纸，捕获完全不受影响，眼睛清净。240×160 在 3.5 寸 960×640 上约 16mm×11mm。听着土，但它是这份约束下唯一**不拿画质换**的办法。

---

## 12. 画质路线的探索结论（2026-08，含失败记录）

这一节记的是**试过并被否掉的方向**，以及否掉它们的数据。结论比代码值钱：不写下来，下一个人（或下一个 AI）一定会把同样的路重走一遍。

起点是一个明确的诉求：**要"重绘感"，一眼看得出是 AI 画的，不追求原汁原味，只追求好看**。目标机型骁龙 8 Elite，可接受十几帧的 AI 刷新率。

### 12.1 DLSS 不是"脑补"，那条路不存在（**限定：这否的是细节，不是稳定性**）

DLSS / FSR2 / XeSS 的本质是**时域累积**：引擎逐像素提供运动矢量、深度、亚像素抖动，它把多帧的**真实采样**对齐叠加。它不编造细节，它在把散落在时间里的信息拼回来。

模拟器只给我们**最终合成的 RGB 帧**——没有运动矢量、没有深度、没有抖动。**DLSS 那条路在架构上就不存在**，不是性能问题。

> **这条结论只对"拿 DLSS 当细节来源"成立，后来被人当成"DLSS 的一切都不可借鉴"用过，那是过度外推。** 稳定性是另一个子目标，需要的输入子集不同：亚像素抖动只为超采样服务，删掉；深度这个仓库现在自己有（第 13 节，写这条时还没有）；运动矢量确实缺，但**这个领域能精确恢复它**——GBA 的 BG 滚动寄存器是整数，捕获是 1:1 原生点采样，所以块匹配的 SAD 能真的等于 0，置信度是免费的。
>
> 不过 13.11 的闪烁最终**不是**靠时域机制解决的，是靠把平移等变性修回来。而且那次测量顺带否掉了重投影的前提：`D(平移(I)) == 平移(D(I))` 在旧网络上差 0.027，历史再怎么对齐也会和当前估计天生不一致。**修完等变性之后这条精确成立**，所以重投影现在是可用的——它剩下的用武之地是补偿推理延迟（第 7 节那条 20~40ms），不是压闪烁。

FSR1 是纯空间算法（EASU + RCAS），不需要额外输入，但它就是边缘导向放大加锐化，和现有的着色器通道同一家族。**能借的只有 FSR3 帧生成的思路**：从相邻两帧的颜色估计运动。2D 游戏的运动绝大部分是全局平移（卷轴），估一个全局位移向量就能覆盖大头。

### 12.2 生成式重绘在 GBA 像素画上失败了，而且是结构性的

选了 Real-ESRGAN anime6B 当教师做蒸馏。结果：

- **预渲染插画/过场效果很好**——线条干净，正中它的先验
- **像素精灵和点阵文字垮掉**——精灵的脸化掉，文字扭曲成乱码

原因是**先验假设错了**。这类模型在动漫**插画**上训练，学到的是"输入是一张被压缩降采样过的平滑画作，我要还原它"。而**像素画不是被降质的插画**——它是作者在这个分辨率上画完的成品，硬边是有意的。模型把精灵轮廓当成压缩痕迹抹平，把 8 像素高的字当成模糊去重建。

试过两个修正：

1. **先用最近邻放大 2 倍再喂教师**。结构在它感受野里变大，不再被当成噪声——**扭曲消失了，但像素感也被抹掉了**。
2. **按边缘掩膜混合**：平坦区用教师重绘，硬边区保留原始像素。

第 2 条**在设计上就是自我抵消的**，实测数字：

```
教师输出 vs 最近邻的差异（混合之前）
  可自由重绘区 (掩膜<0.1):   1.7 / 255
  受保护区   (掩膜>0.9):  15.4 / 255
```

平坦区之所以"安全可重绘"，正因为它平坦、没什么可画；教师真正产生变化的地方（边缘、纹理、笔触）**恰恰是必须保护的地方**。放宽掩膜就毁精灵和文字，收紧就等于没做，**中间没有甜点区**。

> 这和第 3 节早就记录的结论一致（"2D 像素画上小 CNN 打不过比较规则"），只是这次在生成式模型上又验证了一遍。**像素画没有"丢失的细节"可恢复**——这条对判别式和生成式同样成立。
>
> 重绘不是没用，是**用错了地方**。PS1 / 街机那类有真实渐变和抖动的画面，才是它的用武之地。

### 12.3 有效的方向：HD-2D，改光不改像素

真正缺的东西不是分辨率，是**光影**。对比八方旅人可以列出三样：接触阴影/环境光遮蔽、投射阴影、方向性光照。

HD-2D 的本质就是**精灵始终是像素画，被现代化的是环境和光**。它绕开了整个冲突，因为**它不和像素网格打架**——重绘失败是因为"AI 想改的"和"必须保的"是同一批像素；光照改的是另一个维度。

流水线（全部非生成式，可映射到片元着色器）：

```
原生帧 → 最近邻放大（作者的像素一个不动）
       → 深度估计 + 时域平滑
       → 方向光 + 环境光遮蔽（UI 面板排除）
       → 移轴景深 + 泛光 + 调色 + 暗角
```

**代价全免**：没有推理延迟、没有时域沸腾、没有毁文字的风险。

几个实现要点：

- **移轴景深是最标志性的线索，而且不需要深度**——横版 2D 游戏画面上方本来就更远，固定的焦点带就是够用的替代。
- **光照必须在最近邻放大后、其它效果之前做**：它读的是画面结构，先模糊或泛光会把它需要的边缘抹掉。
- **UI 面板不能被打光**。对话框、HUD 是 2D 叠加层，打光会让文字浮雕化。用深度图本身识别：**极近 + 内部完全平坦**，场景里没有真实表面同时满足这两条。

> **曝光必须守恒。** 第一版整体发黑，根因是 lambert 项写成了 `clip(0.5 + g·l, ...)`——梯度为零的平坦区（画面大部分）直接被乘 0.5。基准应该是 1.0。现在强制处理前后平均亮度相等：**光照是重新分配明暗，不是扣掉能量**。这类"多个系数各咬一口、累积把画面吃掉"的问题不该靠调参躲，要从结构上堵死。

### 12.4 深度估计：可用，且比想象中便宜

用 Depth Anything V2 Small 在真实语料上验证（**先看教师输出再决定建不建流水线**，这个纪律在 12.2 省了几小时）：

- **地板、墙体、角色被正确分开** ✓
- **对话框被标成大面积"极近"**——不是错，它确实在最前面，但那是 UI，见上文的排除方法

**深度是低频信息，不需要高分辨率。** 以 960×640 的深度为基准（先用最小二乘对齐缩放+偏移，再比结构残差）：

| 输入分辨率 | 深度结构残差 | 最终打光差异 |
|---|---|---|
| 480×320 | 0.0381 | 2.09 / 255 |
| **240×160** | **0.0369** | **2.17 / 255** |
| 120×80 | 0.0478 | 2.72 / 255 |

**按原生分辨率 240×160 算就够，打光差异 0.85%，肉眼不可辨。** 相比在放大后的 960×640 上算，计算量降到 1/16。

> **相对深度的比较方法**：Depth Anything 输出的是相对深度，对每张图独立 min-max 归一化后直接相减，会把"尺度漂移"混进"结构差异"，得到的数字甚至不单调。必须先最小二乘拟合缩放+偏移对齐。

### 12.5 时域稳定性：轻度抖动，可用 EMA 压住

只在**画面未变化的像素**上比较相邻帧的深度——输入完全相同，输出的任何差异就是估计器自己在抖。移动像素作为对照。

```
未变化像素占比 65%
  静止处漂移  0.0124   ← 纯粹的不稳定
  移动处漂移  0.0468   ← 对照
  比值        0.27
```

静止处漂移只有移动处的 27%，说明模型大体在"读画面"而不是每帧重猜几何。指数滑动平均（`0.8×上一帧 + 0.2×本帧`）即可压住。

**深度比生成纹理好救得多**：深度是低频、变化平缓的，而 GAN 编造的纹理是高频、每帧都不同的，后者根本没法用简单滤波压住。

### 12.6 度量方面的两个教训

**比值不能代替绝对量。** 曾经报告"硬边处差异 0.1、平坦处 1.3，相差 13 倍"当作混合方案成功的证据——但 1.3/255 是 0.5%，**肉眼根本看不出来**。比值很大而绝对值接近零，等于什么都没做。用户一句"怎么感觉没区别"直接戳破。

**先看输出，再建流水线。** 12.2 和 12.4 两次都是靠"先跑几张看看"在几分钟内定生死，而不是训练几小时后才发现方向错了。凡是引入外部模型当教师/先验，**第一步永远是把它的输出打印出来看**。

### 12.7 语料采集工具

采集曾经是第二个启动图标「RetroAI 采集」，**现已删除**——语料采完了，而桌面上多一个图标是要向玩家解释的。采集本身仍然集成在主 APP 里（它需要投影、探测到的取景窗、强制原生输出的 RA 配置，独立工程等于复制整条管线），只是悬浮面板上的入口被撤掉了；`DatasetRecorder` 和 `FloatingBallManager` 里的采集路径都还在，要再采一轮只需加回布局条目。

**存的是原生分辨率像素，不是屏幕截图。** 复用 `Y_EXTRACT` 通道（名字叫 Y，实际输出 RGB），取景窗已吸附成整数倍，按块中心采样取回的就是模拟器原封不动的像素。两种捕获模式下取景窗区域天然干净（整屏模式 discard、单应用模式不在捕获里），**不需要闪屏空帧**。

- **连拍**（长按）15 张 × 130ms，关掉相似度过滤——战斗特效转瞬即逝，2 秒轮询和单击都抓不住。**采集时放宽，过滤放到离线**。
- **反馈显示在按钮上，不用 Toast**：`TYPE_TOAST` 层级 2005 低于覆盖层的 2038，Toast 会被增强画面直接盖住。
- 离线清洗四条规则：纯色、颜色数过少、边框统一色、**全局去重**（设备上只和"上一张"比，隔一小时回到同一场景照样通过）。实测 1786 张原始 → 833 张有效（47%），重复占大头，主要来自连拍——这是预期行为。

> ~~语料的颜色**不落在 RGB555 网格上**（240 种颜色只有 12 种在网格上）……最可能来自 SurfaceFlinger 的色彩管理，**未能确证**。~~
>
> **已结案（2026-08-22），而且答案有两层，第一层是我们自己量错了。**
>
> **① 尺子拿错了。** libretro shim 帧是核心原封不动的输出（逐位比对过，见 NewSolution.md 门 3），而它 **100% 落在 RGB565 网格上、0% 落在 RGB555 网格上**。GBA 硬件是 15 位 BGR555，所以当初假设 555 很自然——**但 VBA-M 的 libretro 核心输出的是 RGB565**，它自己转过了。语料从来就不该落在 555 网格上；那 12 个命中的只是两种展开恰好重合的灰阶。**"颜色被什么东西改过"这个前提，大半是拿错尺子量出来的。**
>
> **② 剩下那部分变换真的存在，但小到不必管。** 同一瞬间、同一画面走两条路取样（shim 干净帧 vs `Y_EXTRACT` 捕获帧），对齐后：
>
> | | |
> |---|---|
> | 逐通道样本 `\|diff\|=0` | **87.19%** |
> | `=1` / `=2` / `=3` | 11.42% / 1.19% / 0.19% |
> | **最大差** | **3 / 255** |
> | 平均差 | 0.144/255 = **0.056%** |
> | 绿通道最受影响 | 26% 像素有差——RGB565 的 6 位绿，符合预期 |
>
> **第 12.6 节自己定的可见阈值是 1.3/255（0.5%），并记着那已经"肉眼根本看不出来"。这里是它的 1/9。** 所以切到 shim 帧源**不需要重采语料、不需要重训模型**，"模型绑定本机显示链"这条顾虑也一并解除。
>
> **③ 顺带量出一件本节没在说的事：捕获路径的取景窗整体偏一行。** 不做这个对齐，两条路的平均差是 14.4；对齐之后是 0.144——**一百倍**。第 2 节"位置故意不吸附，偏 1px 只是整体平移、肉眼不可见"的判断在"可见性"上仍然成立，但它意味着**语料是在偏离原生网格一行的位置上采的**。对训练无害（采到的仍是真实游戏像素），但凡是拿捕获帧和任何别的东西做逐像素比对，**必须先对齐，否则量到的全是错位**。

---

## 13. HD-2D 光影

**当前状态：可用。文字、对话框、UI 面板均已排除；底部暗带已在训练侧解决；移轴景深已实现。实测甜点：光影 75%、景深 50%。**

这一节是给下一个会话的交接。第 12 节记录了"为什么走到这条路"，这一节记录"这条路走到哪了、卡在哪、试过什么"。

### 13.1 它是什么

**不是一个放大引擎，是叠加在任何放大器之上的光照层。** 画面由用户选的引擎（像素边缘重建 / ESPCN / RetroAI）绘制，HD-2D 只用深度图去**重新照明**它。作者的像素一个不改——这正是它在第 12 节那条重绘路线失败的地方能成立的原因。

```
选定引擎绘制画面（像素不变）
  → 深度网络（RetroDepth base，实测 3.3ms）
  → 逐像素自适应 EMA（滞后/闪烁权衡，见 13.3③）
  → 移轴景深（不需要深度，见 13.7）
  → 大尺度方向光 + 距离压暗（smin 软钳位，见 13.6）
  → 叠加层掩膜：红=有边框面板（光照+景深），绿=屏幕锁定内容（仅景深，见 13.4）
```

§12.3 那条流水线的镜头部分（移轴景深 + 泛光）已完成，见 13.7/13.8；调色和暗角明确不做，理由同处。

已知未覆盖：**圆角气泡框**检测不到；**不带框、直接画在场景上的文字**（"LEVEL UP!" 这类）仍会被打光。GBA 开机画面的光斑保留为特性。

UI 是悬浮菜单里的**开关**（长按循环 25/50/75/100% 强度），不是引擎 chip。

### 13.2 深度网络

`RetroDepth base`：32 通道 × 5 残差块，**主体跑原生分辨率（`down=1`）**。3 通道进、1 通道出、不放大。

> **`down=2` 已被否掉，不要为了省算力改回去。** 半分辨率主体（stride-2 头 + PixelShuffle 尾）不是免费的，它的代价是平移等变性，而那就是移动时闪烁的主因——见 13.11。真要省，`--variant tiny`（blocks 5→3）在等变性上完全一样。

| | |
|---|---|
| 成本 | 3.94 GMAC，**实测 20ms** |
| 训练 | 蒸馏 Depth Anything V2 Small，833 张 GBA 帧，200 轮 |
| 结果 | train 0.1403 / val 0.1512，验证集平均绝对误差 24/255 |
| 训练细节 | **整帧训练不裁块**（相对深度逐图归一化，裁块会让归一化不适定）+ 多尺度梯度匹配损失（打光读的是斜率和突变，不是绝对值） |

> **吞吐率不是常数。** RetroSR 实测 236 GMAC/s，RetroDepth 只有 138 GMAC/s——同一块 GPU 差 42%。深度网络每层小（120×80×32）、层数密，固定开销摊不开。**估算不能跨规模外推。**

### 13.3 已解决的问题，以及各自的根因

**① HD-2D 开关完全不生效**

推理是否运行被绑在了"画面走哪条路"上：`if (isAiEnabled_ && !pixelEdgeEnabled_)`。用户选像素边缘重建时，深度网络从未执行。**画面由谁绘制、深度要不要算，是两件独立的事**——现在 `hd2dEnabled_` 独立触发推理。

**② 文字被描黑边、背景大片暗斑**

第一版用**深度突变**算环境光遮蔽。这在真实几何上正确（凹槽就是光进不去的地方），在这里完全错误：**每个字形、每个精灵轮廓都是深度突变**。

> **一张从 2D 合成画面估出的深度图，描述的是"谁挡着谁"，不是"表面朝向哪里"。** 它能承载大尺度起伏和距离，**承载不了投射阴影**——要求它做到就会得到满屏暗斑。

现在只保留两项：大尺度方向光 + 距离压暗（是**雾**不是**投影**，它不描任何轮廓）。

**③ 角色移动时光斑闪烁 —— 这一条曾经被误诊，见 13.11**

原先的结论是"这是一条一维权衡，没有免费午餐"，配了一张 EMA 平均率对滞后/闪烁的表，和五条"试过并失败的破局思路"。**那张表是真的，那个结论是错的。**

闪的不是估计器噪声，是**平移不变性缺失**：网络的 stride-2 头 + PixelShuffle 尾（周期 2），加上着色器里锚定的 mip 读（周期 8）。**对一个确定性振荡做平均，压幅度和拖滞后是同一个系数**——所以那条曲线必然是一维的，五条思路必然全部落在同一条线上。**从来就没有第二个维度可搜。**

完整的诊断、测量和修法见 **13.11**。这里只保留仍然成立的部分：

- **EMA 只对深度做，绝不对画面做**——画面和上一帧混合就是运动模糊，在放大器上是 bug。
- **采样环本身在闪**：原来是 8 个采样点固定在 10 像素半径的圆环上，**移动物体会突然进出某个采样点，平均值跳变**。这是实现缺陷，和网络稳定性无关。改用了 mipmap + `textureLod`——**而这个修法本身引入了 13.11 那条周期 8 的混叠**，现在换成了原生分辨率上的滑动盒。真机 bisect 确认 `sourceBokeh` / `bloomSample` 那两个**至今仍是点采样环的**通道并不闪（把半径放大到 3~4 倍观感不变），所以这个缺陷在它们身上没有显形，暂不改。

**④ 整体发黑**（第 12.3 节也记了）：lambert 基准必须是 1.0 不是 0.5，且强制曝光守恒。

### 13.4 UI 面板排除：按边框判，不按深度判

**这一条被解决前失败过两次，两次都是判据选错，不是参数没调对。**

#### 先修的前置 bug：那个"宽范围模糊"从来没生效过

`ensureAiTextures()` 把 `MIN_FILTER` 设成 `GL_LINEAR_MIPMAP_LINEAR` 之后，**紧接着下一行又设回了 `GL_LINEAR`**。GLES 在非 mipmap 过滤器下**忽略 `textureLod` 的显式 LOD，只采 base level**——所以 `depthWide()` 一直返回的是原生分辨率的原始深度。mip 链每帧都在生成，没有任何人采样它。

后果是双重的：每个字形和精灵轮廓都是原始深度里的突变，梯度项直接把它们描出来（这就是 13.3② 以为已经解决的症状又回来的原因）；同时**下面第 2 次尝试实际上从未运行过**——它写的是读模糊深度，读到的是原始深度，于是它和第 1 次尝试是同一个实验。

> 凡是"某个滤波/模糊看起来没起作用"，先确认采样器状态，再怀疑算法。GL 的状态设置是**最后一次写入生效**，重复设置同一个 enum 不会报错。

#### 深度路线的死刑判决（实测）

用真机同款 `retrodepth_base` 在语料上逐行统计 139 帧的平均深度：

| 行 | 0 | 40 | 80 | 110 | 140 | 155 |
|---|---|---|---|---|---|---|
| 平均深度 | 0.21 | 0.23 | 0.29 | 0.40 | 0.69 | 0.77 |

**RetroDepth 的输出被一条竖直近远斜坡主导**：上三分之二基本恒定在 0.22，然后一路冲到 0.77。所以一个面板的深度值报告的是**它画在屏幕的什么高度**，而不是它是不是 UI。半空中的气泡框读出来是中景，和它背后的场景完全一样。

**这个信号里不存在能把两者分开的切点。** 不是阈值 0.74 选错了，是任何阈值都不行。`uUiNear` 已删除。

#### 现在用的：面板的边框

每个对话框都是一个**闭合矩形**：某一行上一段高对比直线、下方一段两端对得上的直线、两端由竖直线连起来。场景美术也有长直边（地平线、地板、墙），但它们**不会闭合成两端对齐的矩形**。四条屏幕边也算边框，所以贴底的状态条只需找到它的上沿。

实现在 `render/ui_panels.cpp`。209 张跨语料实测：**59% 的帧完全不出掩膜，40% 出 1~45%，1% 超过**。逐张看过，圈中的是对话框、名字条、状态栏、指令窗、道具列表，场景美术没有误伤。

**头像只要画在框里就自动受保护**——交出去的是整个矩形，不是只有平坦的部分。

三个必须保留的实现约束：

1. **检测跑在推理工作线程上**，用深度网络本来就要读的那份原生分辨率缓冲，掩膜和深度因此天然是同一帧。渲染线程持锁跨整个 GPU 帧（10.3a），不能在那里加 CPU 活。实测 0.27ms 均值 / 0.77ms 最差（主机）。
2. **掩膜必须羽化 + 时域平均**，否则它自己的边界就变成一条描边——那正是整个 pass 要避免的产物。
3. **单个矩形不得超过半屏**。四条屏幕边都是边框，没有这条上限时整帧永远是一个合法矩形，pass 会把自己关掉。

> 教训（和 12.6 的"先看输出"同源）：前两次用的都是 UI 的**相关性质**（近、平坦），而"UI 是一个带闭合边框的矩形叠加层"才是**定义性质**。另外，判据失效时先去**测那个信号本身**——把 139 帧的逐行深度打出来，比再调一轮阈值快，而且能给出"这条路走不通"的定论。

### 13.5 深度网络的底部偏置

**现象**：无论什么 GBA 游戏，画面下方都有一块大面积矩形暗带，连纯色 loading 画面上都有。

**根因**：见上表，那条竖直斜坡在 110~145 行最陡（约 +0.015/行，是上三分之二的 5~7 倍）。在 12 像素的差分基线上，**光这条偏置贡献的梯度就足以把 `lambert` 顶到下限钳位**。它来自教师的照片先验（画面下方是地面，地面就近）叠加语料里底部多为对话框，**与当前帧内容无关**。

> **这是训练问题，最终也在训练里解决。** 判据是喂网络一张没有几何的图（纯灰/纯白/噪声），凡输出的都只能来自权重——见模型仓库的 `scripts/probe_bias.py`。三代模型：
>
> | | 纯灰 | 纯白 | 噪声 | 真实帧中位 |
> |---|---|---|---|---|
> | 初版 | 3.42% | 3.30% | 3.34% | 2.75% |
> | +面板掩膜 +`--vflip` | 2.90% | 3.48% | 1.08% | 2.27% |
> | **再 +`--vshift`** | **2.12%** | **2.73%** | **0.81%** | **2.23%** |
>
> 真机结论：**正常游戏画面已看不见暗带**，只在纯色背景（开机 logo、loading 页）上还能看出来——那正是网络无内容可读、只能吐出先验的场景。
>
> 两条方法论：**竖直翻转只能把位置先验对称化，删不掉它**（重训后的模型在无内容输入上返回 U 形而非斜坡），随机上下平移才能让行号不携带信息；以及**"底减顶"这类指标只测不对称，测不到对称的位置先验**，要测的是打光公式产出的逐行亮度斜率。

**这条偏置还不是光滑的**：rows 110~115 和 135~140 各有一个折点，斜率是别处的四倍。宽域均值能减掉整体斜坡，**减不掉折点**，而每个折点就是一条带。

**解法**：梯度改读**减掉逐行均值之后的深度**。

> **横向暗带按定义就是"沿 x 恒定的成分"**，减掉每一行自己的均值就把这一整类精确消除，而沿行方向有变化的结构原样保留。这比"宽域 2D 均值"锋利得多——后者只能削平滑斜坡。

> **那张宽域均值不能来自 mip 链，这条试过并且失败了。** 宽到能装下偏置的 mip 只有两三个 texel（240×160 的第 6 级是 2×3），而这么小的纹理在 clamp-to-edge 下**在图像中间一半以外是常数**——恰好就是暗带所在的位置。实测那一版暗带 +12.8%，比不修（+10.5%）还差一点。**"支撑足够宽"和"在边缘处梯度仍然活着"对 mip 是一对矛盾要求**，所以均值在 CPU 上算（边缘钳位）整张传上去，窗口取 `height/4` 而不是写死像素数。

按"逐行平均亮度的行间变化率"（横带就是这么显形的）实测：

| 方案 | 平均行间斜率 | p99 | 保留的细节 |
|---|---|---|---|
| 不减（原状） | 0.0080 | 0.098 | 100% |
| mip LOD 6 | 无效，见下 | — | — |
| CPU 宽域 2D 均值 | **0.0090（比不减还差）** | 0.099 | 107% |
| **CPU 逐行均值** | **0.0031** | **0.022** | **87%** |

**宽域 2D 均值对暗带毫无帮助**——它只修好了整体亮度，压根没碰到带。而"上方更远"的线索不在这里，由读绝对深度的距离压暗负责。

**但这条补偿最终被关掉了（`uDepthBias = 0`）。** 它确实能压掉带，代价是**删光所有横向延展的结构**，而光照方向有 80% 是竖直的——剩下的只有竖直结构，再放大去凑强度。真机反馈是"很难看"。

> **教训：`sd`（总变化量）是错的度量。** 逐行版本的 lambert sd 是 0.2332，重阴影版本是 0.2298——**数字上一模一样**，观感天差地别。总量相同、**分布**完全不同：一个是大尺度柔和明暗，一个是竖条纹。度量要能区分"多少"和"什么样"。

偏置是**训练**造成的（纯灰图上网络照样输出上下斜坡），所以改为从源头解决——见模型仓库的 `filter_ui_frames.py` 与 `train_depth.py --vflip`。若重训后仍有带，把 `uDepthBias` 设回 1.0 即可恢复这条补偿。

> **踩坑记录（方法论）**：LOD 6 那一版是拿 numpy 的"边缘钳位盒均值"验证通过才写进去的，**但 numpy 模型和 GL 的实际操作不是同一个运算**——真机上完全没效果。教训：**验证必须打在实现上，不是打在实现的模型上。** 现在 `common/depth_profile.cpp` 是独立编译单元，宿主机上和 numpy 逐像素对拍（最大误差 1e-5），CI 之外也能随时重跑。

**基底纹理用 R16F 不用 R8**：量化到 8 bit 平均吃掉 0.43% 的 lambert、p99 2.1%，而基底是**平滑场**，这种误差表现为平坦区的等高线色带而不是噪点。半浮点把它压到 0.03%。

**距离压暗（`far`）仍读绝对深度**，不读高通值：它要知道的就是距离，而那条"上方更远"的全局渐变正是 12.3 想要的线索。

### 13.6 底部暗带的真正来源：`far` 的硬钳位

上面 13.5 那条竖直偏置是真的，高通也确实修好了它——**但暗带不是它造成的，两次高通都没打到点上。**

暗带来自距离压暗那一行的**硬钳位**：

```glsl
float far = 1.0 - clamp((1.0 - dC) * uOcclusion, 0.0, 0.22);   // 旧
```

实测：**58% 的像素恰好被钉在 0.22 这个上限上**。也就是说整个画面上三分之二的 haze 是一个常数，完全不携带深度信息；等深度越过阈值后它才突然开始变化，`d(far)/dy` 从约 0 一步跳到 0.0025/行。而 13.5 那条偏置保证了这个越界点**在每个游戏里都落在同一高度**——于是每个 GBA 游戏底部都有同一条横线。

它是**斜率不连续（C1 折痕）**，不是阴影。`lambert` 那边怎么改都碰不到它，因为它根本不在 `lambert` 里。

**把 `far` 场单独渲染出来一眼就看见**：上方一整块均匀灰板，下面一条笔直的边。凡是"某个产物怎么修都还在"，先把**产生它的那个中间量本身画出来**，不要只看最终画面。

**解法**：钳位换成**光滑最小值**（二次 smin），远离拐点处与 `min()` 完全相同，拐点处斜率连续。

```glsl
float softMin(float a, float b, float k) {
    float h = max(k - abs(a - b), 0.0) / k;
    return min(a, b) - h * h * k * 0.25;
}
float far = 1.0 - softMin((1.0 - dC) * uOcclusion, uHazeCap, uHazeKnee);
```

> **顺带的曝光教训。** 13.5 的高通同时把画面整体提亮了约 6%（平均乘数 0.821 → 0.874），观感发白。原因是**旧 lambert 里那条系统性负梯度一直在顺手压暗画面**，大家习惯的亮度其实是那个产物的副作用。现在 `uOcclusion 0.55 / uHazeCap 0.22 / uHazeKnee 0.20` 把平均乘数**有意**定在 0.784——是三个数字明说的结果，不是某个 bug 的残留。

> **这段原先写的是 0.70 / 0.30 / 平均乘数 0.826，和 13.10 的表以及着色器本身都对不上。** 模型仓库的 `hd2d.py` 也停在 0.70 / 0.30，于是**离线预览比真机暗约 10%**（平均乘数 0.706 vs 0.784，逐像素差 19.8/255）。这是"两边靠人工同步"第三次失效——前两次见 13.5 和 13.11。现在 `tools/check_shading_parity.py` 每次构建比对全部光照常数，不一致就构建失败。

### 13.7 移轴景深

§12.3 里"最标志性的线索，而且不需要深度"——因此它是独立开关，网络关掉也能用。

**它故意模糊画面上下部。** 项目里其它任何地方这都是缺陷，这里是效果本身，是明确决定过的。

两条实现要点：

- **模糊的是源，不是成品。** 一块区域反正要虚化，就没必要先对它跑边缘重建——直接跳过那条昂贵路径，也不需要第二个 pass。
- **必须双环，不能单环。** 单环是一圈采样点、不是圆盘，超过两三个 texel 就变成 8 个鬼影而不是模糊——实测**加大半径几乎无效**（4 倍半径只把画面改变量从 1.15% 提到 1.83%）。补一圈半径减半的内环把圆盘填实后，半径 6 才像镜头；单环时代的 2.2 是看不见的。

**叠加层豁免用判决式而不是按比例**：羽化是为了让**光照**边界不变成描边，但半虚化的面板不显得"柔"，只显得**像玻璃**。

### 13.8 泛光，以及为什么不做调色和暗角

和移轴是一对镜头效应，同样不需要深度。12 次采样、双环、阈值 0.72、半径 10 原生 texel。**实测甜点 25%**——50% 时高光铺得太开，读作"盖在画面上的雾"而不是"从画面里发出来的光"。

> **整条链路必须在线性光下做。** 第一版在 sRGB 空间取阈值，然后对**累加出来的增量**调用 `pow(2.2)` 再相加——**增量不是颜色**，把它当颜色解码会直接把它压没：平均超出量 0.06 变成 0.002，**约 47 倍太小**，四个档位在屏幕上完全一样。现在阈值和累加都在线性空间里做，并且用 gamma 2.0（`x*x` / `sqrt`）而不是 2.2：省下每片元 13 次 `pow`，而一个即将被摊到 60 个输出像素上的量，2.0 和 2.2 的差别经不起看。

**在光照之前采集、在光照之后叠加**：散射发生在镜头里、不属于场景，不该被场景自身的明暗压暗。

**叠加层不发光**——对话框足够亮会跟着发光，看起来明显不对：它是画在玻璃上的，不是透过玻璃照进来的。掩膜读一次，景深和泛光共用。

#### 调色和暗角：明确不做

**调色**是这张清单上唯一**和作者打架**的效果。泛光和景深**模拟镜头**，叠在任何画风上都成立；调色是**替作者重新决定配色**，而这批游戏的调色板本来就是各自美术方向的一部分。

**暗角**同理否掉——它只适合比较严肃的游戏，套在明快画风上不对味。

成本：整层让 GPU 从 22~32% 涨到 **60%**，深度推理 9ms。可接受。

### 13.9 屏幕锁定叠加层检测（无边框 HUD）

面板检测器只认有边框的东西，血条那类没有。用 HUD 的**定义性质**：场景在动而它不动。

两条守卫都是语料逼出来的：

1. **只在场景真的在动时学习**——否则玩家站着不动，一秒内整屏都变成 HUD。
2. **"在动"按变化的_铺开程度_判，不按_量_**。镜头平移改变几乎每一个格子；几个大精灵原地动作改变很多像素但只在局部。实测：只看像素数量会把静止镜头场景的 **31~34%** 判成叠加层，改看铺开程度后归零，真实检出全保留。

> **已知局限：零视差背景层也是屏幕锁定的**，按这个定义它就是叠加层。"必须有硬边"能去掉平坦天空（24%→9%），但**有细节的远景层仍会被判成 HUD**。所以它**只驱动景深**——天空少了虚化，比光照被挖洞便宜得多。掩膜因此是两通道：红=面板（光照+景深），绿=屏幕锁定（仅景深）。

### 13.10 关键参数位置

全部在 `gl_renderer.cpp` 的合成着色器和 uniform 推送处：

```
uShadeStrength  hd2dStrength_（UI 可调，默认 0.75 —— 实测甜点）
uShadeRadius    已废弃  曾是 mip 级别；depthWide 现在读 CPU 算的滑动盒（13.11）
uDepthBaseTex   CPU 算的逐行均值（1×h 纹理），减掉沿 x 恒定的成分（13.5）
uRelief         6.0    方向光强度
uDepthBias      0.0    逐行偏置补偿开关，当前关闭（见 13.5）
uOcclusion      0.55   距离压暗
uHazeCap        0.22   压暗上限，用 smin 逼近而不是 clamp（13.6）
uHazeKnee       0.20   smin 拐点宽度——设 0 就是硬钳位，暗带立刻回来
uDofStrength    dofStrength_（UI 可调，实测甜点 50%）
uDofCentre/Band 0.56 / 0.20   焦点带中心与半高
uDofRadius      6.0    全离焦时的模糊半径，原生 texel
uBloomStrength  bloomStrength_（UI 可调，实测甜点 25%）
uBloomThreshold 0.72   低于此不发光；调低了整幅画会发光，读作雾
uBloomRadius    10.0   泛光半径，原生 texel
uDofStrength    dofStrength_（UI 可调，实测甜点 50%）
uBloomStrength  bloomStrength_（UI 可调，实测甜点 25%）
uDofStrength    dofStrength_（UI 可调，默认 0.5 —— 实测甜点）
uDofCentre      0.56   清晰带中心（略低于画面中心）
uDofBand        0.20   全清晰带的半高
uDofRadius      6.0    全虚化处的模糊半径，原生 texel
uLightDir       (-0.55, -0.80)  左上，HD-2D 惯用方向
kDepthSmoothing 0.55   深度 EMA，固定值（gl_renderer.h）。**它现在柔化的是两帧精灵
                       动画的跳变，不是估计器噪声**——后者已在 13.11 从源头解决。
                       逐像素自适应门已删除。
```

**75% 是实测甜点**（真机逐档验证）。早先记的"50% 是甜点、100% 满屏暗斑"**已作废**：那是在 pass 还在给每个字形描边时标定的，当时调低的是**那个产物**，不是光。

### 13.11 移动时的闪烁：平移不变性，不是时域噪声（2026-08-19）

**这一节推翻 13.3③。** 那条记的"一维权衡、没有免费午餐"是误诊，五条失败的破局思路全部因为同一个原因失败。

#### 症状怎么定的性

玩家的描述是关键，比"闪烁"这个词有用得多：**"往上移动过程中，阴影外形在变化，像水波浪一样上下波动，而且是往复波动。"**

匀速滚动配上**往复**振荡，这不是噪声——噪声不会回来。往复意味着输出是**内容相对某个固定网格的相位的确定性函数**，而这有周期。管线里有两个东西有周期。

#### 先做的排除：真机 bisect

在悬浮球上加了个长按循环，把 HD-2D 预设拆成分量（光照 / 景深 / 泛光 / 全关），并把两个点采样环的半径放大 3~4 倍。**放大半径这一步是判据本身**：正确的低通支撑越宽越稳，欠采样的点环支撑越宽越糟，**两个假设预测的符号相反**。

结果：光照单独复现全部闪烁；景深、泛光、全关都不闪；半径放大无区别。**采样环假设当场排除**，闪烁 100% 在深度光照通路上。

#### 两个混叠源，周期都对上了

用 `RetroAI-Model/scripts/probe_shift.py`：把帧平移、重估、再平移回来。等变的管线应该原样返回。

| 位移 | 网络（深度误差） | mip 读（打光误差） | 滑动盒 |
|---|---|---|---|
| 1 | **0.0260** | 0.0058 | 0.00003 |
| 2 | 0.0025 | 0.0104 | 0.00004 |
| 4 | 0.0033 | **0.0139** | 0.00005 |
| 8 | 0.0046 | **0.0012** | 0.00006 |
| 12 | 0.0052 | **0.0139** | 0.00006 |

- **网络：奇数 0.0274 / 偶数 0.0041，完美的周期 2 方波。** `head` 是 3×3 stride-2 卷积（无抗混叠预滤波），`tail` 用 PixelShuffle 从**不同通道**合成 2×2 的输出相位，两个相位之间没有任何约束。内容移 1 像素就换一个格子被分析、换一组权重被重建。
- **mip：0 → 0.0139（半格 4）→ 0.0012（整格 8）→ 0.0139，完美的周期 8 三角波。** `textureLod(depth, uv, 3.0)` 是锚在纹理网格上的 8×8 抽取，内容在动盒子不动。
- **滑动盒：全程 0.00005，平的。**

按 1px/帧、60fps 折算：网络 30Hz（细碎），mip **7.5Hz（慢波浪）**。而且 **mip 在 EMA 之后生成，时域平均根本够不到它**。

> **架构层面的证据**：用**未训练的随机权重**测（纯架构，无训练成分），`down=2` 奇数位移 0.349 / 偶数 0.000086，比值 **4051×**；`down=1` **每个位移都是精确的 0.000000**。训练把 0.349 压到了 0.027，但那是它永远够不到 0 的地板。

#### 为什么五条思路必然全败

**对确定性振荡做平均，压幅度和拖滞后是同一个系数。** 那条曲线是一维的，因为它就是这一个系数在动。特别地，#5（`--vshift` 训练增强）"位移测试改善但闪烁纹丝不动"是这个诊断的指纹：**整像素平移增强把两个相位平均，合并不了它们——混叠在算子里，不在权重里。**

#### 修法，以及为什么必须两个一起

- **网络**：`down=1`。stride-1 卷积栈对整像素平移严格等变，是构造上的。代价 4 倍本体算力（0.99 → 3.94 GMAC，真机实测 **20ms**）。
- **着色器**：`depthWide()` 不再读 mip，改成在推理线程上算原生分辨率滑动盒（`boxDepthField`），整张上传。mip 链**整个删掉**。

|  | 旧模型 | 新模型 |
|---|---|---|
| mip 读（旧实现） | 0.049 | 0.020 |
| **滑动盒（新实现）** | 0.051 | **0.0064** |

**只修一个都不行**：只换模型剩 3 倍；只改着色器几乎没用（0.051，甚至比 mip 略差——mip 的 8×8 抽取碰巧一直在替网络的混叠打掩护）。两个一起，峰值 0.049 → 0.0064，**7.7 倍**，而且曲线从"来回跳"变成"单调爬升后压平"。**单调漂移不闪，来回跳才闪。**

> 网络修好之后 mip 项反而**涨了 47%**（0.0139 → 0.0204）：`down=1` 的深度场结构更多，锚定抽取混叠得更狠。

#### 为什么这个缺陷在离线藏了这么久

`hd2d.py` 把这一读建模成 `_box(depth, 4)`，注释写着"~ mip level 3"。**它不是 mip level 3。** 盒均值平移不变，锚定 mip 不平移不变。**所有离线评估跑的都是一个不可能表现出这个缺陷的滤波器。**

这正是 §13.5 那条"验证必须打在实现上，不是打在实现的模型上"第二次以同样形状撞上来。现在有工具兜着：`tools/check_shading_parity.py` 在宿主机编译 `depth_profile.cpp` 和 numpy 逐像素对拍（实测 8.3e-06），**已接进 Gradle**。

#### 连带：EMA 的含义变了

混叠没了之后，真实估计器噪声从 0.01457 掉到 **0.00618**（`probe_smoothing.py`，只用镜头静止的帧对——旧口径把滚动场景对"上下文移动"的**正当响应**也算成了不稳定）。

平均率因此不再是降噪，而是**柔化两帧精灵动画的跳变**（花团随风摆动那类）——那是输入真的变了，网络跟着变是对的，但不平均的话光照会在两个状态间可见地跳。**0.55 是真机从"不平均"往下走到跳变不再碍眼的第一档**，取这个区间里最大的 α：抖动在 0.35~0.55 之间几乎是平的（0.00491 → 0.00567，肉眼分不出，真机确认），而滞后翻倍（16ms → 37ms）。

逐像素自适应门**已删除**。它读 `|新 − 旧|`，正是它要去噪的量，从来分不清估计器抖动和场景移动；而现在网络等变，这个差值每次都是真信号。

### 13.12 重投影补偿：深度是一个"零阶保持"的采样

**13.11 修完之后剩下的那一半。** 症状是光影随移动整体滑开一截、停下又慢慢回位，边缘还在一两个像素内来回。

#### 根因

**画面是实时的，深度不是。** 画面走像素边缘重建，读的是当前捕获帧（零延迟）；深度是最近一次推理完成的结果，描述的是**提交那一帧**。推理提交间隔实测 **28ms**（不是按推理耗时估的 20ms），深度上屏时年龄 28~56ms。

两次结果之间画面继续滚而深度冻着，错位一路涨，新结果落地又掉回去——**约 50Hz 的锯齿被 60Hz 采样**。按真机时序模拟，逐帧着色残差 **0.0158（保持）对 0.0003（补偿）**，56 倍。

排除掉的两个：8bit 历史（float 历史 0.01574 vs uint8 0.01576，**完全一样**，9×9 盒平均早抹平了）、EMA 速率（关掉只差 7%）。

#### 运动矢量是"恢复"的，不是估的

GBA 滚动寄存器是整数，取景窗吸附整数倍、按块中心采样，所以**正确偏移下两帧逐比特相同，SAD 严格等于 0**。残差因此是免费的置信度，没有阈值要调。`common/scroll_estimator.cpp`，跑在推理线程上（和面板检测器同理，10.3a）。

> **这在 `down=1` 之前是不成立的。** 平移深度的前提是 `D(平移(I)) == 平移(D(I))`，旧网络在奇数位移上差 0.027。

#### 两个踩坑，都是真机日志发现的

**① 用置信度去缩放补偿量，是错的。** 置信度说的是"**这一对帧**能不能测出位移"，补偿量说的是"**自深度那一帧以来**场景移动了多少"——一对读不出来的帧对第二个量什么都没说。乘在一起的后果是一帧读不出就把补偿整个归零，画面弹回未补偿位置，**比原来的锯齿更大更快**。现在置信度只决定要不要**更新速度**。

**② 速度不能用 EMA 平滑，要用窗口累加。** 游戏按整像素滚动，所以每次量到的整数**就是真实位移**；慢走实测 `2,3,2,1,3,2,3,2,2,2,3,2,1,3,2,3`，均值 2.2/间隔。EMA 会永久残留 √(α/(2−α)) 的噪声——α=0.5 时 16%，乘 32ms 年龄 = **1.6 像素的补偿量摆幅**，实测 1.50/1.58/1.60/1.65/1.69/1.69，与预测的 1.6 吻合。改成 8 个间隔累加后速度误差从 ±20% 降到 **±6%**。

> 停止不能等窗口（会拖 200ms），连续两次高置信度零位移直接清空。

#### 剩下的地板

游戏按**整像素**阶梯前进，补偿按平均速度**连续**外推，一个间隔内两者差 **±0.5 像素**，频率是游戏帧率。真机描述是"半个像素、非常快、几乎感受不到"。再往下要每显示帧一次 GPU 回读来读出当前整数滚动位置，**为半个像素不值得**。

#### 度量上的教训

`Shift:` 日志里那个 swing（~1.1px）**不是 artifact 的幅度**，它本来就该存在——深度更新时内容前进一整个间隔，补偿量同时降回去，两者相消。我拿它当判据并据此预测"会掉到 0.2~0.4"，**预测错了**，因为量错了东西。真正该量的是相消之后的残差。**一个会随正确行为一起变化的指标，不能用来判断正确性。**

#### NaN 不会被 clamp 挡住

`std::min`/`std::max` 遇到 NaN 返回哪个操作数取决于比较顺序，**NaN 能直接穿过一对钳位**到着色器，`uv - NaN` 让采样未定义、光照变 NaN，多数驱动写出来是**黑色**。而覆盖层的安全底线是退化成全透明、绝不黑屏（第 1 节）。现在钳位前先 `std::isfinite`。

---

## 14. 面向玩家的形态（改 UI 前先读）

这一节记的是**产品形态上的决定**。它们没有一条是技术约束，但每一条都有理由，而且都很容易被"顺手改回去"。

### 14.1 引擎是"一种画面"，不是"一堆开关"

光照、移轴景深、泛光曾经各有一个开关和一条长按调强度的循环。那在**调参阶段是对的**，调完就是错的：玩家在选的是"要哪种画面"，不是在用三个滑块拼一种画面。

现在它们是 `UpscaleEngine.HD2D` 这一个选项，和 ESPCN 那些并列，**没有任何参数可调**。

**引擎必须独占它的效果**：`applyEnginePreset()` 在选择引擎和启动时都会跑，把 `hd2dStrength / dofStrength / bloomStrength` 设成该引擎携带的值——其它引擎全设 0。少了这一步，旧配置里存着的效果会在换引擎后留在屏幕上。

### 14.2 被隐藏但保留的两个引擎

`UpscaleEngine.isHidden` 挡住的两个，**都不是死代码**：

- **RETROAI**（重绘）——12.2 那个否定结论是**针对像素画**的。PS1 / 街机那类有真实渐变和抖动的画面才是它的用武之地，权重也还在 assets 里。
- **DEPTH**（深度图调试视图）——第 13 节几乎每一轮排查都用到它。

存档里如果读到已隐藏的引擎，`OutputProfileManager.load` 会**回落到像素边缘重建**——否则菜单显示一个、渲染器在跑另一个。

### 14.3 "启用 AI 超分"必须关掉所有东西

这个开关曾经**完全无效**。`uAiEnabled` 只管合成着色器里 GPU 锐化那条分支，而它排在 `uPixelEdge` 后面——默认引擎恰恰是像素边缘重建，于是开关够不到任何东西。

现在它会一并清掉像素边缘重建、深度推理、光照、景深、泛光，让合成落到 `sampleSourceSharp`：**游戏原样放大，什么都不加**。复古质感模拟那一栏不受影响，它是独立的一组。

> 教训：`if / else if` 链里，**排在后面的条件只有前面全不成立时才可达**。加分支时要检查这条链的顺序，不是只看自己那一条对不对。

### 14.4 倍率对没有网络的引擎无意义

"AI 超分倍率"是**网络输出纹理的尺寸**。像素边缘重建、HD-2D、GPU 锐化都没有网络——它们直接采样捕获帧，画多大由几何决定（屏幕能容下的最大整数倍，第 3 节）。

所以选中这些引擎时那几个 chip 会**置灰**。一个还能点、但点了什么都不发生的控件，和一个 BUG 无法区分。

### 14.5 采集入口已撤，代码留着

语料采完了，而桌面上多一个图标是要向玩家解释的。`CaptureActivity` 和第二个启动项已删除；采集本身仍在主 APP 里（`DatasetRecorder` + `FloatingBallManager` 里的采集路径），只是面板上没有入口。**再采一轮只需要加回布局条目，不是重写。**

### 14.6 图标：只有中间 72 是安全区

自适应图标画布是 108×108，但**只有中心 72 保证可见**——外圈每边 18 是给启动器遮罩裁掉的出血。第一版蓝圆画成直径 80，于是边缘被切。现在是 58。

改图标时还要**同时改 `versionCode`**：启动器按包名+版本缓存图标，同版本号 `install -r` 刷新不了。这件事花掉过一整轮真机往返，而 APK 里的资源从头到尾都是对的。

---

## 15. 推理耗时的测量（2026-08 实测，先读这节再报任何 ms）

这一节记的是一次跨机种延迟排查。**结论不是某个优化，是"之前所有的 ms 都不可信"**，包括本文档里写过的几个数。

### 15.1 延迟跟的是原生帧缓冲像素数，跟被模拟机器的强弱无关

起因是一个看起来违反直觉的观察："性能开销越小的机器，AI 延迟越高"。那是看错了轴。网络在原生分辨率上跑逐像素卷积栈，它只看到一张多大的图：

| | 原生 | 像素 | HD2D 实测 | 折合吞吐 |
|---|---|---|---|---|
| GBC | 160×144 | 23,040 | 5.8 ms | 407 GMAC/s |
| GBA | 240×160 | 38,400 | 10.3 ms | 382 |
| SFC | 256×224 | 57,344 | 19.8 ms | 297 |
| FC | 256×240 | **61,440** | 21.0 ms | 300 |

FC 是这四台里帧缓冲**最大**的，GB 最小，排序完全吻合。吞吐在这个尺寸区间内缓慢下垂约 25%，ESPCN Ultra 同档比较下垂 24%——**两个网络行为一致，没有断崖**。

### 15.2 耗时是双峰的，两档相差恒定的 2.1 倍 —— 那是 GPU 时钟

装上中位/p95/GPU/CPU 四联报数之后才看清楚。**判据不在任何单场测量里，在跨会话对同一配置的复测里**：

| HD2D | 会话 B | 会话 D 快档 | 会话 D 慢档 | 慢/快 |
|---|---|---|---|---|
| GBC 160×144 | 5.8 | — | 12.1 | **2.09** |
| GBA 240×160 | 10.3 | — | 22.9 | **2.22** |
| SFC 256×224 | 19.8 | 19.7 | 44.0 | **2.23** |
| FC 256×240 | 21.0 | 21.1 | 47.5 | **2.25** |

会话 D 里 GBC/GBA 那两个"稳定值"，正好是会话 B 的**两倍**；SFC/FC 的快档和会话 B **完全一致**。ESPCN Ultra 同样：FC 4x 是 16↔35（2.19），SFC 4x 是 17↔33（1.94）。

**四个机种、两个结构完全不同的网络（32 通道 26 层 vs 48 通道 8 层）、四次会话，比值全是 2.0~2.25。** 一个跟机种、跟网络、跟画面尺寸都无关的全局速度因子，只能是**时钟**。

两条否定结论同样值钱：

**① 不是 CPU。** CPU 段实测恒定 **0.1~0.3 ms**，四个机种、两个网络、包括 FC 6x 那个 220 万输出像素的配置，全都一样。`from_pixels`/`to_pixels`/下载都不值钱，工作线程也从没被踢下大核。**全部的耗时和全部的方差都在 `ex.extract()` 里面。** 这条是那四行分段计时买来的。

**② "只有大画面才跳"是采样错觉，别信。** 会话 D 里 GBC/GBA 的 p95 紧贴中位（12.1/12.3、22.9/24.9），看起来稳如磐石——它们只是**整场都锁在慢档**。小画面 GPU 占用低，调速器的升频门限根本不触发；SFC/FC 的负载正好卡在门限上，所以来回抖。同一台 GBC 在会话 B 报 5.8、在会话 D 报 12.1，中间什么都没改。

> **一个紧的 p95 只说明"只采到了一个状态"，不说明"这就是成本"。** 这是本节最容易再犯一次的错。

**③ 快档才是正常的，慢档是缺陷。** 快档吞吐 SFC/FC 是 298~350 GMAC/s，GBC/GBA 是 382~407；慢档掉到 133~195。**13.2 里那句"RetroDepth 只有 138 GMAC/s"，量的就是慢档——本文档一直在把坏状态当常数记录。**

还没做的是**把时钟钉住**（没有 root，见第 7 节）。在那之前唯一能跨机种比的量是**地板**，见 15.3。

### 15.2b 时钟之外还有第二件事：GBA 和 SFC 之间有一级台阶

把时钟因子剥掉之后剩下的东西。**两种独立测法**——正常运行取地板（15.3）和满载 burst 取最小（15.8）——对每个配置各取最小值：

| | 像素 | HD2D | 吞吐 | Ultra 4x | 吞吐 |
|---|---|---|---|---|---|
| GBC | 23,040 | 5.8 ms | **407 GMAC/s** | 4.2 ms | **500 GMAC/s** |
| GBA | 38,400 | 10.3 | **382** | 7.3 | **479** |
| SFC | 57,344 | 19.2 | **306** | 14.8 | **353** |
| FC | 61,440 | 20.6 | **306** | 15.5 | **361** |

**这不是 15.1 说的"缓慢下垂 25%"，是一级台阶。** 台阶内部几乎完全一致（HD2D 407/382 差 6%、306/306 差 0%；Ultra 500/479 差 4%、353/361 差 2%），台阶之间掉 21% 和 27%，**两个网络的落点都在 GBA（38,400 像素）和 SFC（57,344 像素）之间**。

> 15.1 那句"吞吐缓慢下垂约 25%"作废——那是把台阶当成了斜坡，因为当时手里只有混着时钟档的数。

两个结构完全不同的网络在同一个位置踩到同一级台阶，而它们唯一相同的是**每次推理同时存活的张量大小**（32ch×3 ≈ 48ch×2，巧合但真实）：

| | HD2D 32ch | Ultra 48ch |
|---|---|---|
| GBC | ~4.2 MB | ~4.2 MB |
| GBA | ~7.0 | ~7.0 |
| SFC | ~10.5 | ~10.5 |
| FC | ~11.2 | ~11.3 |

**界在 7.0 和 10.5 MB 之间**，看着像 8 MB。这是本项目目前唯一**可能改得动**的那部分成本（时钟改不动，没有 root）：切横条推理能把工作集压回界内，halo ≥ 层数时输出逐位相同，因此不破坏深度通道必须的平移等变（13.11 / `down=1`）。FC 切两条 120+13 行，工作集从 11.2 掉到约 6.2 MB，代价约 11% 的重叠算力。

**切横条已经试过，被否掉了。** 见 15.9。

### 15.3 因此 HUD 报中位数和 p95，不报瞬时值

`stats_.aiMs` 现在是最近 32 次推理的**中位数**，旁边是 p95、地板、以及 GPU 段（ncnn `extract`）和 CPU 段（`from_pixels`/`to_pixels`）各自的中位数。

- **地板（`aiFloorMs`）**是模型加载以来见过的最小窗口中位数。设备只要访问过一次高频档它就落到位，之后不再回升——**这是唯一可以跨机种、跨会话比较的数**；
- **中位数**只说明"此刻在哪一档"，不能拿来比配置（15.2②）；
- **中位数到 p95 的差距**是慢档占了多少时间；
- **GPU/CPU 分段**回答"哪一半在动"——这一条直接否掉了整条 CPU 侧的猜想，代价是四行代码。

> 地板用窗口中位数而不是单次最小值：一次走运的结果会把地板永久钉在一个不存在的值上，而地板是只降不升的。

窗口在换模型、`unloadEspcn()`、失败退化三处**清零**。旧的数活过它描述的对象，和 10.7 那个 `success=1` 是同一类错误。

> **一个双峰的量，瞬时读数是抛硬币。** 13.2 那句"估算不能跨规模外推"还得再加半句：**在报任何 ms 之前，先确认它不是双峰的。**

### 15.4 那几轮把人绕进去的配置串味

三个独立的坑叠在一起，让同一个引擎的数据被当成四个机种的对比：

1. **profile 全部按机种独立存盘**（`key(console, …)`），切机种会静默恢复那台机器上次的引擎和倍率。HD2D 跑完一轮，四个机种的 `engine` 就全被写成 HD2D 了——"每次切换平台都默认锁定 HD-2D"不是默认值（默认是 PIXEL_EDGE），是上一轮被还原回来。
2. **`ProfilePreference.save()` 曾经也写 `KEY_CONSOLE`**，而悬浮窗持有的是服务启动时的快照。于是在主界面选完新机种后，在菜单里碰任何一个控件都会把机种**改回上一个**并重新盖一遍设置。已删除：**谁拥有一个 key，谁就得是它唯一的写入者。**
3. **机种在运行中切换不生效**，且无法从界面看出来。服务只在启动时读一次（`applyConfigOnStart`），悬浮窗在构造时快照一次，没有任何东西通知运行中的服务。渲染器继续按旧机种的原生分辨率推理，RA 也停在旧机种的取景窗上——这就是"切完机种打开 RA 是全黑的"。现在运行中直接**拒绝切换**，并提供「停止并选择」。

> **`adb shell am force-stop` 不能用来重启服务。** 停止服务时的 RA 配置还原跑在独立的非 daemon 线程上（第 2 节），force-stop 正好把它掐死在半路，RA 的核心配置里留着上一个机种的取景窗——全黑的另一半原因。要重来就点「停止 AI 增强」，等通知栏常驻项消失。

### 15.5 未解决：亚像素尾的通道数不是 8 的倍数

第 5 节把"通道数必须是 8 的倍数"列为硬约束，但**尾部卷积的输出通道是 s²，导出器管不到**：

| 倍率 | 尾部通道 | elempack |
|---|---|---|
| 2x | 4 | pack4 ✗ |
| 3x | 9 | **pack1** ✗✗ |
| **4x** | **16** | **pack8 ✓** |
| 6x | 36 | pack4 ✗ |

四档里只有 4x 合规。15.2 测出来它只值约 1 ms，所以**不是当前的瓶颈**，但它是一条写在文档里、实际被绕过的约束。要修得回模型仓库改导出器（把尾部补齐到 8 的倍数再截断），本仓库改不了。

### 15.6 顺带：倍率仍然要玩家猜，而 6x 在 FC/SFC 上是显示不出来的

1080p 上 6x 的重建结果 FC 是 1536×1440、SFC 是 1536×1344，**高度都超过 1080**，算完直接丢掉，还白占 2.2 MB 纹理和每次出结果的一次上传。第 5 节那条待办（倍率跟着实测几何自动钳制）还没做，这是它的实测代价。

### 15.7 地板还是靠运气

**地板还是靠运气。** 它只在设备当场访问过高频档时才落到位，而设备经常整场不升频。同一个 GBA + HD2D，一轮地板 22.1、下一轮 10.3；GBC + Ultra 一轮 4.2、下一轮 7.4。**要跨会话比，只能把每个配置在多轮里的最小地板取出来**，本节所有表格都是这么来的。这不是一个能交给用户的流程。

### 15.8 校准按钮（已实现，但它不控制时钟）

悬浮菜单最下方的「校准 AI 耗时」。**这是诊断入口，不是玩家功能**——它报的数玩家既看不懂也用不上，之所以放在悬浮菜单而不是主界面，只是因为它必须在管线跑着、模型已加载的时候才有意义。

做法：工作线程在**publish 一个正常结果之前**接下请求，拿手上那一帧**连续推理 3 秒**，排序后报最小/中位/最大和次数。三件事一次解决：

- 占空比 100% ⇒ 调速器必须升频，**量到的是高频档**；
- 不和 RA、合成通道交错 ⇒ 没有争用；
- 3 秒一格，四机种 × 两引擎不到一分钟。

**实测：它排除了争用，但控制不了时钟。而且两种形状都试过了。**

- **连续满载 3 秒**：八格里四格整场待在慢档，每格正好是自己已知最快值的约 2 倍（GBC HD2D 11.5 vs 5.8、GBA HD2D 21.5 vs 10.3、GBC Ultra 8.1 vs 4.2、SFC Ultra 28.3 vs 14.8）。下一轮四个对照格里又中了三个。"连续满载 ⇒ 调速器必须升频"这个前提**是错的**。
- **改成"空闲 200ms + 连跑 8 次"× 十几轮**：理由是移动 GPU 的加速档通常给"空闲后的短突发"。**结果更差——八格全部落在慢档**，而且轮内 max/min 从 1.3 涨到 2.0 以上（每轮都从低频重新爬）。已回滚。

保留连续形状，只因为它**轮内更干净**（max/min 1.23~1.40），单次读数更可信；落哪一档仍然是抛硬币。所以：

- `aiCalMinMs` **跨按压累计**，只在换模型时清零。按三次读「最快」；
- 按钮上同时显示 **`aiFloorMs`（运行中最快）**。**真正抓得到高频档的是它，不是校准**——它在正常游玩时累积，而校准跑的时候整条渲染管线是停着的。两个数并排看，取小的那个。

> **测量工具本身也会有先验，而且一样会错。** 这一节两次设计都基于"我知道调速器怎么想"，两次都被数据打脸。工具做完的第一件事是拿已知答案的格子验它，不是直接拿它去测未知。

几个不能改的地方：

- **3 秒不能缩短。** 调速器按几十毫秒的窗口采样忙碌率，要连续几个窗口都满才升档。跑 300ms 量到的正好是要逃开的那个空闲档。
- **必须在 publish 之前跑。** 先 publish 会让渲染线程立刻提交下一个作业，然后那个作业挂在队列上三秒。
- **循环里要查 `aiShutdown_`。** `stopAiWorker()` 会 join 这个线程，三秒的 join 落到主线程上就是 ANR（10.3c）。
- **换模型时清零**（在 `resetAiTimings()` 里）。校准结果描述的是当时加载的那个网络。**这里不能加锁**：失败退化那条路径已经持有 `aiMutex_`。

同一轮改动里 HUD 退回**只显示一个数**（滚动中位数）。地板/p95/GPU/CPU 全部照算不误，只是不再摆给玩家看——**五个统计意义不同的数字放在玩家菜单里，对两边受众都更糟**。要看它们就按校准。

在有校准结果之前，**不要引用任何绝对 ms**，也不要基于 15.2b 那级台阶动手改推理结构——量不准就判不出改动是好是坏。12.6：先看输出再建流水线。

### 15.9 切横条推理：试过，否掉了

15.2b 那级台阶的解释是"工作集越过了某个内存侧的界"，直接推论是把画面切成横条、让每条都落回界内。实现过一版：halo 从 .param 里算出来（每个卷积的 `(k-1)/2 × dilation` 求和，RetroDepth base 13 行、ESPCN Ultra 7 行），给足半径则切块输出与不切块在数学上相同，因此不破坏深度通道必须的平移等变；只对 >38,400 像素的画面生效，所以 GBC/GBA 走的是逐字节相同的老路径。

**实测无效。** 那一轮八格全部落在慢档（见 15.8），所以只能同档比：

| | 未切块 慢档 | 切块 慢档 | halo 成本 |
|---|---|---|---|
| SFC HD2D | 134 GMAC/s | 126 | +11.6% |
| FC HD2D | 133 | 125 | +10.8% |
| SFC Ultra 4x | 185 | 162 | +6.2% |
| FC Ultra 4x | ~160 | 142 | +5.8% |

**吞吐没有向 GBC/GBA 那一档移动，净变化就是多付的 halo。** 台阶比值（SFC ÷ GBA）从 0.78 变成 0.69，只降不升。横版过关游戏在接缝处没有断带，说明 halo 给对了——**这不是实现错误，是假设错误**。

代码已删除（连同 `tools/check_tiling.py` 和它的构建门）。留在这里的是结论：

1. **台阶是真的，但它不是"每次推理的峰值张量"。** 把峰值砍半没有任何效果。剩下的候选是总内存流量（切块不减反增）、或者某个跟画面尺寸相关但和峰值无关的东西。
2. **halo 白名单那一版差点白跑。** 第一版 `isTileSafeLayer` 漏了 `TanH`——fast 和 hq 两个模型的最后一层——于是那两个引擎的切块从第一行代码起就是关着的，**不报错、不改画面、只是白多花钱**。白名单 fail closed 且无声，加白名单就必须同时加验证它的门。那个门（编到宿主机上逐模型问 halo）当场抓到了它，也抓到了我写错的两个预期值。门随代码一起删了，但**下次再写任何"按类型决定要不要走快路径"的开关，先写门**。
3. **改动的收益必须能被当前的测量手段分辨。** 这次预期收益 14~24%，而测量本身有 2.1 倍的时钟噪声——只是因为收益是负的、且负得比 halo 成本还多，结论才勉强成立。**收益小于噪声的改动，做完也判不出好坏。**

### 15.10 探测只打一枪，而 GBA 那一枪总是空的

自动探测在目标应用进前台 **1.5 秒后执行一次**，`detectSourceWindow()` 内部轮询 3 秒拿不到结果就静默放弃，**没有重试**。整个 session 就此不再测量，增强画面停在按预测的取景窗算出来的位置上。

**只有 GBA 中招**：1.5 秒时它还在放 BIOS 开机 logo——满屏黑底加一个滑进来的小字样。阈值化之后最大的非黑连通域就是那个字样，长宽比闸门（10.2）正确地毙掉它。其余机种载入即出整屏画面，一次就中，所以这个缺陷藏了很久。

> **拒绝是对的，拒绝一次就放弃不对。** 10.2 那条"宁可拒绝也不能放行"只说了判据该多严，没说失败之后该怎么办。

现在失败会重试：`AUTO_DETECT_ATTEMPTS = 5`、间隔 `AUTO_DETECT_RETRY_MS = 4000`，覆盖约 20 秒，一旦锁上立刻停。间隔必须大于 `detectSourceWindow()` 自己那 3 秒轮询——两次探测同时在飞会互相把对方要测的帧擦掉。可见代价是探测失败期间悬浮球会闪几下（探测必须撤掉自己的窗口），而那段时间画面本来就在错的位置上。

---

## 16. 中英双语（2026-08）

英文是**默认**（`values/`），中文在 `values-zh/`。方向是故意的：`values/` 是所有非中文语言的兜底，日语或德语系统拿到英文，而不是一门它们大概率读不懂的语言。**"跟随系统"因此不需要任何代码**——安卓自己按目录选。用 `values-zh` 而不是 `values-zh-rCN`，繁体系统会拿到简体，那是两个错误答案里较好的那个。

### 16.1 手动切换必须包装 Service 的 Context，不能只包装 Activity

用 `attachBaseContext(LocaleHelper.wrap(newBase))`，**`MainActivity` 和 `OverlayService` 两处都要**。

> **只包装 Activity 的表现是"主界面英文、悬浮窗中文"，而且看起来像漏翻译。** 玩家在游戏里看到的一切——悬浮菜单、每一条 Toast、通知栏——都是从**服务**的 context 取的，不是 Activity 的。第一版漏了这一处，排查时的关键线索是"跟随系统正常、手动切换才错"：跟随系统那条路径根本不需要包装。

不能用 `AppCompatDelegate.setApplicationLocales`：它驱动的是 Activity 重建，**API 33 以下对 Service 无话可说**，而 minSdk 是 30——两台实测机正好都在它够不着的区间里。

**切换语言不重启服务。** 悬浮菜单的视图在服务启动时 inflate，那一次的字符串就定死了。为一行文字把用户的取景窗和已写好的 RA 配置拆掉不划算，所以改成弹一句"重启 AI 增强后悬浮菜单也会跟着切换"。

### 16.2 枚举的显示名是资源，不是构造参数里的字面量

`ConsoleType` / `SourceCorner` / `UpscaleEngine` / `MaskType` 现在带 `@StringRes labelRes` 和 `label(context)`。

改之前确认过它们**只用于显示和日志**：目录名、SharedPreferences 的 key 走的都是 `enum.name`。**如果哪个枚举的显示名参与过持久化，改它就等于让已有数据全部失联**——这是动这类枚举前唯一要查的事。日志里原来用 `displayName` 的两处改成了 `name`：**日志不该跟着界面语言变**。

### 16.3 别拿本地化文案当逻辑判据

`OverlayService` 里曾经有一行：

```kotlin
if (result.ok && result.message.startsWith("已备份")) { ... }
```

用**用户可见文案**决定要不要弹提示。中文单语时它一直是对的，翻译上线的那一刻必然静默失效——而且失效方式是"提示不弹了"，没有任何报错。现在 `RetroArchBackupManager.Result` 带 `createdSnapshot: Boolean`，判这个。

**凡是 `Result(ok, message)` 这种形状，`message` 就只能给人看。** 需要分支就加字段。

### 16.4 备份规则文件曾经自相矛盾

`data_extraction_rules.xml`（API 31+）排除一切，而 `backup_rules.xml` 是个空的 `<full-backup-content>`——**空的意思是全部备份**。minSdk 30 走后者，所以同一个 App 在安卓 11 上全量备份、在 12 上一点不备份。而且排除项还漏了必需的 `domain` 属性，本身就是非法的。

现在只有 `android:allowBackup="false"` 一句，两个文件都删了。排除一切本来就是原意，只是写错了地方两次。

> **`lintVitalRelease` 只在 release 构建跑**，所以这个错误一直藏到第一次打正式包才暴露。日常 `assembleDebug` 永远看不到它——发版前至少跑一次 `./gradlew lintVitalRelease`。
